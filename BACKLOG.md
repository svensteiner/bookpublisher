# BookPublisher BACKLOG

Verbesserungen die den Agenten in Richtung 9,5/10 bringen.
Ein Item pro Run vollständig implementieren — kein Halbzeug.

## KRITISCH (direkter Qualitätssprung für den Autor)

- [x] **Kapitel-Analyse:** Jedes Kapitel einzeln bewerten — Versprechen, Beweis, Wert, Übergang. Output pro Kapitel: Score + konkrete Fix-Zeile. *(modules/chapters.py + chapter_review() in modules/review.py, wired into pipeline run_qa)*
- [x] **Konkrete Rewrite-Vorschläge:** Nicht "Titel zu kurz" sondern 3 alternative Titel mit Keyword-Score und Kaufmotivation. Gilt für Titel, Untertitel, Amazon-Description. *(modules/rewrites.py: build_rewrite_report() + render_rewrite_report_markdown(), pure-Python, in run_qa pipeline integriert — artifacts: rewrite_suggestions.md/.json)*
- [x] **Amazon-Description-HTML:** Generator der die KDP-Beschreibung in echtem Amazon-HTML ausgibt (`<b>`, `<br>`, Bullet-Liste) — copy-paste-fertig für KDP Backend. *(modules/amazon_html.py: build_amazon_description_html() + render_amazon_description_report_markdown(), HTML auf KDP-erlaubte Tag-Subset beschränkt + HTML-Escaping, in run_qa pipeline integriert — artifacts: amazon_description.html/.json + amazon_description_report.md)*
- [x] **7 KDP-Keywords konkret befüllen:** Statt "Keywords fehlen" → liefere die exakten 7 Keyword-Strings (max 50 Zeichen each) basierend auf Titel, Subtitle, Beschreibung. *(modules/kdp_keywords.py: build_kdp_keywords() + render_kdp_keywords_report_markdown(), pure-Python, KDP-Regeln durchgesetzt: ≤50 Zeichen, lowercase, Umlaute zu ae/oe/ue/ss, keine subjektiven/Marketing-Tokens, keine Titel-Wiederholung. In run_qa pipeline integriert — artifacts: kdp_keywords.md/.json)*
- [x] **First-10%-Deep-Scan:** Die ersten 10% des Manuskripts sind der Kindle-Sample. Bewerte jeden Abschnitt: Hält der Leser durch oder bricht er ab? Hook-Stärke, Versprechen-Klarheit, erster konkreter Wert. *(modules/sample_scan.py: build_sample_scan_report() + render_sample_scan_markdown(), pure-Python, vier Achsen Hook/Versprechen/Wert/Lesbarkeit, Abbruch-Risiko-Ampel pro Abschnitt. In run_qa pipeline integriert — artifacts: sample_scan.md/.json)*
- [x] **Runden-Gedächtnis mit Delta:** In Runde 2+ prüfen ob die Fixes aus Runde 1 umgesetzt wurden. *(modules/round_delta.py: RoundDelta + compute_round_delta() + render_round_delta_markdown(); AgentMemory.compare_rounds() in modules/agent_core.py; in run_qa pipeline integriert — artifacts: round_delta.md/.json. Klassifiziert Fixes als erledigt / persistent / neu und zeigt Score- + Investor-Grade-Delta inkl. Decision-Wechsel.)*

## WICHTIG (professionelle Qualität)

- [x] **LLM-Fallback:** Wenn claude-sonnet-4-6 fehlschlägt → claude-haiku-4-5-20251001 als automatischer Fallback mit Logging. *(modules/llm.py: `_call_model()` als testbarer Helper extrahiert, `complete()` versucht primaries Modell, fällt bei Exception automatisch auf `config.fallback_model` zurück, loggt `model_call_started`/`model_call_error`/`model_fallback_started`/`model_fallback_completed`. Bei doppeltem Fehlschlag ein klarer `ConfigError` der beide Modelle und beide Fehlertexte nennt — kein Traceback für den End-User. Edge Cases: kein Fallback konfiguriert, Fallback identisch mit primary, explizites Modell-Argument als Override — alle abgedeckt durch tests/test_llm.py.)*
- [x] **Strukturierter Score-Verlauf:** JSON-Datei die über alle Runden den Industrial-Score trackt. Graph-fähig (Datum, Score, Top-3-Fixes). *(modules/score_history.py: ScoreHistoryEntry (frozen dataclass) + load_score_history()/append_score_history()/render_score_history_markdown(); pure-Python, immutable append-only mit Cap auf 50 Einträge. Jeder Eintrag trägt timestamp, round_id, mode, decision, industrial_score, investor_grade, gate-Snapshot (name/status/score), Top-3-Fixes und score_delta gegen den vorigen Eintrag. In run_qa-Pipeline integriert — artifacts: score_history.json (graph-ready: Datum + Score + Delta + Top-Fixes pro Runde) + score_history.md (Tabelle + Sparkline-Trend `▁▃▅▇`). Robust gegen kaputtes JSON, fremde project_ids, fehlende QA-Felder. Auch in QUICK_ROUND_ARTIFACTS und Mirror-Liste eingetragen, damit der Kunde den Verlauf direkt im Reportordner sieht. 13 Unit-Tests in tests/test_score_history.py.)*
- [x] **Kapitel-Reihungscheck:** Prüfe ob die Kapitelreihenfolge dem klassischen Sachbuch-Bogen folgt (Problem → Lösung → Beweis → Transformation). *(modules/chapter_arc.py: `ChapterPhase` + `ArcReport` (frozen dataclasses) + `build_arc_report()` + `render_arc_report_markdown()`; klassifiziert jedes Kapitel über deterministische Marker-Regex auf eine der vier Phasen (Problem/Lösung/Beweis/Transformation) mit kleinem Positions-Bias als Tie-Breaker, berechnet einen 0–100 Arc-Score über alle geordneten Paare (Anteil der Paare in kanonischer Reihenfolge) und zieht 15 Punkte pro fehlender Phase ab. Fehlende Phasen und Reihenfolge-Inversionen werden mit konkreten Fix-Zeilen für den Autor benannt. Integration in `run_qa` via `chapter_arc_review()` in modules/review.py, neue Artefakte `chapter_arc.md` + `chapter_arc.json`, in `QUICK_ROUND_ARTIFACTS` und Mirror-Liste eingetragen. 13 Unit-Tests in tests/test_chapter_arc.py decken kanonische Reihenfolge, Inversionen, fehlende Phasen, Position-Bias-Fallback, Konfidenz und Markdown-Render ab.)*
- [x] **Competitive-Positioning-Prompt:** Basierend auf Titel + Beschreibung generiere "Was macht dieses Buch einzigartig vs. die Top-Wettbewerber in dieser Nische?" Als eigener Review-Abschnitt in publisher_board_review. *(modules/competitive_positioning.py: `PositioningReport` + `CompetitorArchetype` + `DifferentiationAngle` (frozen dataclasses) + `detect_niche()` + `build_positioning_report()` + `render_positioning_markdown()`; pure-Python, deterministisch, neun Nischen-Vokabulare (KI, Finanzen/CFO, Vertrieb, Produktivität, Führung, Selbständigkeit, Immobilien, Mindset, Allgemein) mit je 3–4 Wettbewerber-Archetypen. Sechs Differenzierungs-Achsen werden über Regex-Signale aus Titel+Untertitel+Beschreibung gescort (Zahlen-Beweis, Anti-Hype, Operator-Stimme, Methode/Playbook, Anti-Berater, Spitze Zielgruppe); Top-3 Angles werden nach Stärke sortiert ausgegeben. Kollisions-Risiken (fehlende Zahlen, fehlende Zielgruppe, Hype-Titel, KI-/Immobilien-Spezialregel) und ein copy-paste-fähiger Positionierungs-Satz werden mitgeliefert. Integration: in `run_qa` (pure-Python, artifacts: competitive_positioning.md/.json) + in `run_review` als zusätzliche Kontextsektion für `publisher_board_review`. PUBLISHER_BOARD_PROMPT um expliziten "Wettbewerbs-Positionierung"-Block erweitert, der den vorgerechneten Report aufgreift. In QUICK_ROUND_ARTIFACTS, FULL_ROUND_ARTIFACTS und der Mirror-Liste eingetragen. 15 Unit-Tests in tests/test_competitive_positioning.py decken Nischen-Erkennung, leere Metadaten, Signal-getriebene Angle-Keys, Sortierung nach Stärke, Cap auf 3, Hype-Titel-Risk, Pitch-Inhalt, JSON-Roundtrip und Markdown-Rendering ab.)*
- [x] **Leser-Persona-Generator:** Aus Titel + Beschreibung + Inhaltsverzeichnis automatisch 3 konkrete Buyer-Personas ableiten (Alter, Job, Problem, Kaufmotiv). Output in amazon_research_brief.md. *(modules/personas.py: `BuyerPersona` + `PersonaReport` (frozen dataclasses) + `build_persona_report()` + `render_persona_report_markdown()` + `render_persona_brief_section()`; pure-Python, deterministisch. Wählt aus 9 nischen-spezifischen Persona-Baselines (3 pro Nische) anhand der Niche-Detection und verfeinert jede Persona mit Signal-Flags (B2B, Selbständig, Einsteiger, Zeitdruck, Proof) sowie Anker-Keywords aus Metadaten + Kapitel-Titeln (TOC). Jede Persona trägt Label, Altersband, Job, Problem-Statement, Kaufmotiv und eine plausible Amazon-Suchanfrage. Output: eigener Report `buyer_personas.md/.json` plus eingebetteter Compact-Block in `amazon_research_brief.md` (3 nummerierte Personas mit Verweis auf Detail-Report). In `run_qa` Pipeline integriert (chapter_titles aus chapter_review.json wiederverwendet, kein Doppel-DOCX-Read), in QUICK_ROUND_ARTIFACTS + Mirror-Liste eingetragen. 15 Unit-Tests in tests/test_personas.py decken Persona-Anzahl-Cap, Immutability, Längen-Limits für Problem/Motiv/Quote, leere Metadaten-Fallback, Signal-Flags (Proof/B2B/Selbständig), Nischen-Selektion (KI vs. Finanzen), TOC-Anker-Integration, Markdown-Rendering, Brief-Section-Empty-Handling, JSON-Roundtrip und Audience-Extraction ab.)*

## POLISH (letzter Schliff)

- [x] **Einheitliche Score-Darstellung:** Alle Gates nutzen dieselbe Skala und dasselbe Farbschema in beginner_summary.md (🟢 ≥85, 🟡 65–84, 🔴 <65). *(modules/industrial.py: zentraler `score_badge(score, blocking)`-Helper als einzige Quelle der Wahrheit für die Schwellen 85/65 und die Emojis 🟢/🟡/🔴. `_status` ruft jetzt `score_badge` auf, damit es nie auseinanderläuft. Neuer `## Gate-Übersicht`-Block in `render_beginner_summary` listet alle Gates mit Badge + Score + Status + lesbarem deutschen Label (`GATE_DISPLAY_LABELS`); blockierende Gates (status=FIX trotz Score ≥65) werden auf 🔴 gezwungen. Score-Zeile im beginner_summary trägt jetzt auch das passende Badge. `render_industrial_qa_markdown` setzt das Badge konsequent vor jeden Gate-Header. Skala wird oben sichtbar dokumentiert (`Skala: 🟢 ≥85 · 🟡 65–84 · 🔴 <65`), damit der Autor die Bedeutung sofort erkennt. 9 neue Unit-Tests in tests/test_industrial.py decken die 85/65/<65-Grenzen, blocking-Override, Gate-Übersicht-Präsenz, deutsche Labels, Markdown-Badge-Präfix, Score-Zeile-Badge, leere Gate-Liste sowie die Konsistenz zwischen beginner_summary und industrial_qa_report ab.)*
- [x] **.env.example aktualisieren:** ANTHROPIC_API_KEY dokumentieren + Kommentare welche Features welchen Key brauchen. *(`.env.example` von obsoletem `OPENAI_API_KEY=` auf vollwertig dokumentiertes `ANTHROPIC_API_KEY=` umgestellt: Console-Link, klare Feature-Matrix (Schnellrunde `qa` = kein Key, `review`/`cover`/`launch`/`round --full`/`all` = Key noetig), Kostenschaetzung pro Vollreview, Hinweis auf automatisches Sonnet→Haiku Fallback. 7 neue Unit-Tests in tests/test_env_example.py sperren den Kontrakt: Datei-Existenz, ANTHROPIC_API_KEY-Deklaration, leerer Placeholder-Wert (keine echten Keys versehentlich committed), kein obsoletes OPENAI_API_KEY mehr, Features-mit-Key und Features-ohne-Key sind dokumentiert, Console-Link vorhanden. 218 Tests insgesamt grün.)
- [x] **Fehler-Meldungen verbessern:** Wenn ein DOCX nicht gelesen werden kann → klarer Text welche Datei fehlt und was der User tun soll. Kein Python-Traceback für den End-User. *(modules/readers.py: neue Exception `ManuscriptReadError(RuntimeError)` mit Pfad+Grund+Hinweis, gemeinsamer Helper `_open_docx_document()` wrapped `PackageNotFoundError`/`BadZipFile`/`PermissionError`/`FileNotFoundError`/`IsADir` in eine deutsche User-Message mit konkretem nächsten Schritt. chapters.py / industrial.py / sample_scan.py nutzen jetzt den gemeinsamen `open_docx_paragraphs()` Entry-Point. GUI (`gui.py`) zeigt für `ManuscriptReadError` einen eigenen Dialog-Titel „Manuskript konnte nicht gelesen werden". CLI (`modules/cli.py`) gibt Exit-Code 3 mit lesbarem Text aus. 16 Unit-Tests in tests/test_readers.py decken alle Fehlerpfade ab und prüfen, dass kein Traceback-Text in den Messages auftaucht.)*

## Neu entdeckt (während Implementierung gefunden)

- [x] **Kapitel-Scoring mit LLM verfeinern:** Aktuelle Bewertung ist heuristisch (Regex-Marker). Optional eine LLM-Pass-Variante für reichere Fix-Vorschläge — laufzeit-gesteuert über AppConfig. *(modules/config.py: neues `AppConfig.chapter_review_llm_fixes_enabled: bool = False` Feld + Loader-Pfad; config.yaml dokumentiert (Off-by-Default, jeder LLM-Fehler fällt stillschweigend auf den heuristischen Pfad zurück). modules/chapters.py: `ChapterScore` um optionale `llm_fix: str = ""` + `fix_source: str = ""` Felder erweitert (frozen, default leer); `to_json()` lässt beide weg wenn leer. Die deterministische `fix`-Zeile wird NIE überschrieben — beide Varianten landen im Report, damit der Autor den generischen Hinweis mit der konkreten Empfehlung vergleichen kann. Neue Bausteine analog zum sample_scan-LLM-Pass: `FIX_SOURCE_LLM`/`FIX_SOURCES`, `LLM_FIXES_SYSTEM_PROMPT`, `LLM_FIXES_MAX_CHAPTERS=6` (Cost-Cap), `LLM_FIXES_BODY_PREVIEW_CHARS=400`, `LLM_FIXES_MAX_FIX_CHARS=280`/`LLM_FIXES_MIN_FIX_CHARS=20` (Quality-Gate). Pure helper `build_chapter_fixes_user_prompt(report, chapter_bodies, *, limit, preview_chars)` listet NUR schwache Kapitel (status REVIEW/FIX) mit Titel + Score + schwächster Dimension (deutsches Label via `_DIMENSION_LABELS`) + heuristischem Fix-Hinweis + Body-Preview, sortiert nach overall ascending (schlechtestes zuerst); leerer String wenn nichts zu enrichen ist → Caller short-circuited den LLM-Call. `_parse_chapter_fixes_payload` extrahiert `{index: fix}` tolerant gegen Shape-Drift, filtert Ausrufezeichen + zu kurze Strings, kappt zu lange via Ellipsis. `extract_chapter_fixes_via_llm` schluckt Exceptions zu leerem Dict (kein abgebrochener Lauf). `apply_chapter_fixes(report, fixes, *, source)` baut ein neues immutables `ChapterReport` via `dataclasses.replace` — Original wird nie mutiert, gibt sogar die Original-Instanz zurück wenn das Mapping leer ist oder kein Eintrag tatsächlich etwas ändert (whitespace-only). Unbekannte sources fallen auf `FIX_SOURCE_LLM` zurück. Pure helper `chapter_bodies_from_paragraphs` reuset `split_paragraphs_into_chapters` → kein Off-by-one zwischen Prompt- und Report-Index. `render_chapter_report_markdown` zeichnet die `- LLM-Fix:`-Zeile nur wenn gesetzt. modules/review.py: `chapter_review` in `build_chapter_review_report()` + `render_chapter_review()` aufgespalten (backwards-kompatibel), exponiert das Report-Objekt, damit die Pipeline den LLM-Pass VOR dem Rendern fahren kann. modules/pipeline.py: neuer Helper `_maybe_apply_chapter_llm_fixes(project, report)` mit vier Gates (toggle aus, kein API-Key, keine schwachen Kapitel, kein Manuskript), loggt `chapter_review_llm_fixes_skipped/_failed/_completed`; `run_qa` ruft build → maybe-apply → render, sodass die LLM-Fixes in `chapter_review.json` UND `.md` landen. Jede Exception aus DOCX-Read/LLM-Call führt zu Pass-Through ohne Crash. 29 neue Unit-Tests (21 in tests/test_chapters_llm.py + 8 in tests/test_pipeline_chapter_llm.py) decken Felder/JSON, Prompt-Filterung+Sortierung+Limit+Dimension-Label, Parser-Robustheit, apply-Immutability, extract-Short-Circuit/Exception-Swallow, Index-Alignment, Markdown-Render und alle vier Pipeline-Gates + Happy-Path (monkeypatched extract_docx_chapters) ab. 951 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Per-Kapitel Wortzähl-Balance:** Wenn ein Kapitel >3× das Median-Volumen hat, flaggen ("Kapitel X ist viermal länger als der Durchschnitt — splitten?"). *(modules/chapters.py: `ChapterBalanceOutlier` + `ChapterBalanceReport` (frozen dataclasses) + `analyze_chapter_balance()` + `_render_balance_section()`. Pure-Python, deterministisch. Verwendet `statistics.median` und flaggt Kapitel >3× Median als SPLIT-Kandidat (rot) sowie <0.3× Median als MERGE-Kandidat (gelb). Mindestens 3 Kapitel erforderlich, sonst leerer Report (kein meaningful median). Outliers sortiert: oversized nach absteigender Wortzahl (längstes zuerst), undersized nach aufsteigender Wortzahl (kürzestes zuerst), Tie-Break über Kapitel-Index. Jeder Outlier trägt eine konkrete Fix-Zeile mit Kapitel-Titel, Wortzahl und Median-Verhältnis ("Kapitel X ist 4× so lang wie der Durchschnitt — splitten"). `ChapterReport` um optionales `balance`-Feld erweitert (backwards-kompatibel, Default `None`); `build_chapter_report` populiert es und hängt die Balance-Fixes an die Gesamt-Fixliste. `render_chapter_report_markdown` rendert eine neue Sektion `## Kapitel-Balance` mit Split/Merge-Untergruppen — Sektion wird komplett weggelassen, wenn keine Outlier vorliegen. 16 neue Unit-Tests in tests/test_chapters.py decken: flagging oversized/undersized, Sortier-Reihenfolge, Tie-Break über Index, Minimum-Chapter-Schwelle, leere Eingabe, keine Outliers, Immutability, ChapterReport-Integration, Fix-Anhängung, Markdown-Render (sichtbar + versteckt), JSON-Serialisierung (mit + ohne Balance). Alle 227 Tests grün.)*
- [x] **Beginner-Summary erweitern:** Top-3 schwächste Kapitel mit ihrer Fix-Zeile direkt in beginner_summary.md übernehmen. *(modules/chapters.py: neuer pure helper `top_weakest_chapters(report, limit=3)` (deterministisch, tie-break über Kapitel-Index, immutable). modules/industrial.py: `render_beginner_summary` nimmt jetzt optional `weakest_chapters: list[dict] | None`. Neuer Sub-Renderer `_render_weakest_chapters` zeichnet die Sektion `## Schwächste Kapitel` mit `score_badge`-Emoji, Score und Fix-Zeile — Sektion wird komplett weggelassen wenn keine Daten vorliegen. modules/pipeline.py: neuer pure helper `_weakest_chapter_payload(chapter_json, limit=3)` extrahiert die schwächsten Kapitel aus `chapter_review.json`, sortiert by (overall, index), clamped auf verfügbare Kapitel, tolerant gegen partial dicts. `run_qa` reordered: chapter_review wird jetzt VOR beginner_summary berechnet, damit die Top-3-Daten in die Summary fließen — chapter_review-Artefakte werden weiterhin geschrieben. 9 neue Unit-Tests: 6 in tests/test_chapters.py (Sortierung, Tie-Break, Clamping, leerer Report, Limit 0, Immutability) + 3 in tests/test_industrial.py (Sektion sichtbar, fehlt bei None/[], robust gegen partial dicts) + 6 in tests/test_pipeline_helpers.py (None-Report, leere Liste, Sortierung, Clamping, Limit 0, partial dicts). 204 Tests grün.)*
- [x] **Rewrite-Varianten mit LLM verfeinern:** Aktuelle Varianten folgen festen Bestseller-Mustern. Optional eine LLM-Pass-Variante die das Original direkt umschreibt (statt Template) — schaltbar über AppConfig. *(modules/config.py: neues `AppConfig.rewrite_llm_variants_enabled: bool = False` Feld + Loader-Pfad; config.yaml dokumentiert (Off-by-Default, jeder LLM-Fehler fällt stillschweigend auf den Template-Pfad zurück). modules/rewrites.py: `RewriteOption` um `source: str = "template"` Feld erweitert (Provenance, `to_json` emittiert es immer — Subset-Tests bleiben grün). Neue Konstanten `REWRITE_SOURCE_TEMPLATE`/`REWRITE_SOURCE_LLM`/`REWRITE_SOURCES` (analog `rewrite_source` in sample_scan), `LLM_VARIANTS_SYSTEM_PROMPT` (Anti-Hype, kein Ausrufezeichen, Anker-Keywords behalten, Zeichen-Limit respektieren), `LLM_VARIANTS_PER_FIELD=2` (Cost-Cap), `LLM_VARIANTS_DEFAULT_MOTIVATION`. `build_rewrite_report` bleibt rein (3 Template-Optionen) — die LLM-Varianten werden separat über `apply_rewrite_variants` ANGEHÄNGT (nie ersetzt), damit der Autor Template vs. Original-Rewrite vergleichen kann. Pure helper `build_rewrite_variants_user_prompt(report)` listet NUR Felder mit Diagnose-Finding (READY-Felder brauchen keine Copy-Arbeit) mit Original + Zeichen-Limit + Problemen + Anker-Keywords; leerer String wenn nichts zu rewriten ist → Caller short-circuited den LLM-Call. `_parse_rewrite_variants_payload` extrahiert `{field: [(text, motivation), …]}` tolerant gegen Shape-Drift, filtert unbekannte Felder, Non-String-Text, Ausrufezeichen (Anti-Hype), zu-kurze Texte (unter Feld-Minimum), kappt zu-lange via `_clip` auf das Feld-Maximum (TITLE/SUBTITLE/DESCRIPTION_LEAD_MAX_CHARS), cappt auf `LLM_VARIANTS_PER_FIELD` pro Feld, defaultet fehlende Motivation. `extract_rewrite_variants_via_llm` schluckt alle Exceptions zu leerem Dict (kein abgebrochener Lauf). `apply_rewrite_variants(report, variants, *, source)` baut ein neues immutables `RewriteReport` via `dataclasses.replace` — Original nie mutiert, gibt die Original-Instanz zurück wenn das Mapping leer ist oder kein Eintrag tatsächlich etwas anhängt (whitespace-only Text clippt zu leer → übersprungen); `keyword_score` wird gegen die Report-Anker neu berechnet, unbekannte sources fallen auf `REWRITE_SOURCE_LLM` zurück. `render_rewrite_report_markdown` zeichnet `- Quelle: LLM-Pass (direkt aus deinem Original umgeschrieben)` nur bei LLM-Optionen. modules/pipeline.py: neuer Helper `_maybe_apply_rewrite_variants(project, report)` mit drei Gates (toggle aus, kein API-Key, keine schwachen Felder) — arbeitet rein auf Metadaten, KEIN DOCX-Read nötig; loggt `rewrite_llm_variants_skipped/_failed/_completed`. `run_qa` ruft build → maybe-apply → to_json, sodass die LLM-Varianten in `rewrite_suggestions.json` UND `.md` landen und auch `_top_rewrite_payload` (beginner_summary Top-Pick) eine stärkere LLM-Variante wählen kann. 37 neue Unit-Tests (26 in tests/test_rewrites_llm.py + 11 in tests/test_pipeline_rewrite_llm.py) decken Source-Feld/JSON, Prompt-Filterung+leer-ohne-schwache-Felder+Anker-Fallback, Parser-Robustheit (unbekanntes Feld/Ausrufezeichen/zu-kurz/zu-lang-clip/Cap/Default-Motivation/Non-Dict/Non-String), extract-Short-Circuit+Exception-Swallow, apply-Immutability+Source-Stamp+Score-Recompute+Whitespace-Skip+untargeted-Bundle-unverändert+Description-Max-Chars, Markdown-Marker, Config-Default+Toggle-Read und alle Pipeline-Gates + Happy-Path + LLM-Failure-Fallback. 988 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Rewrite-Top-Pick in beginner_summary:** Top-Variante (höchster Keyword-Score) automatisch in beginner_summary.md vorschlagen. *(modules/industrial.py: neuer Sub-Renderer `_render_top_rewrite` zeichnet `## Top-Rewrite-Pick` mit `score_badge`-Emoji, deutschem Feldlabel (Titel/Untertitel/Beschreibungs-Einstieg), Quote-Block des Rewrite-Textes, Keyword-Score, Zeichenzahl und optionaler Motivation. `render_beginner_summary` nimmt jetzt zusätzlich optional `top_rewrite: dict | None`. Sektion wird komplett weggelassen, wenn keine Daten vorliegen ODER der Text leer ist — kein leerer Quote-Block. modules/pipeline.py: neuer pure helper `_top_rewrite_payload(rewrite_json)` selektiert nur Bundles mit Diagnose-Findings (also Felder, die wirklich überarbeitet werden müssen), wählt aus diesen die Option mit höchstem Keyword-Score, tie-break über kürzeren `char_count` (punchiger Text gewinnt) und stabile Feld-Priorität (title > subtitle > description_lead). Pipeline reordered: `build_rewrite_report` wird jetzt VOR `render_beginner_summary` aufgerufen, das vorab berechnete `rewrite_json` wird an die nachfolgende Persistenz weitergereicht — kein doppelter Bau. 11 neue Unit-Tests: 7 in tests/test_pipeline_helpers.py (None-Report, leere Bundles, keine Diagnose → None, höchster Score gewinnt, Tie-Break char_count, Tie-Break Feld-Priorität, leere Text-Options, Immutability), 4 in tests/test_industrial.py (Sektion sichtbar mit Score+Motivation+Filelink, fehlt bei None/empty, fehlt bei reinem Whitespace-Text, kein "Warum:"-Line ohne Motivation). 249 Tests grün, kein bestehender Test gebrochen.)
- [x] **KDP-HTML Live-Preview im beginner_summary:** Kurze Vorschau der ersten 3 Zeilen (gerendert, ohne HTML-Tags) damit der Autor sieht, wie die Beschreibung auf Amazon aussieht. *(modules/pipeline.py: neuer pure helper `_amazon_html_preview_payload(amazon_html)` extrahiert ein immutables Dict mit `headline`, `lead`, `bullets` (Tuple, cap auf 2 via `AMAZON_HTML_PREVIEW_MAX_BULLETS`, leere/whitespace-only Bullets übersprungen), `char_count`, `keyword_score`. Returns `None` für None-Input oder wenn `headline`, `lead` und `bullets` alle leer sind — Section wird komplett skipped wenn nichts zu zeigen ist. modules/industrial.py: neuer Sub-Renderer `_render_amazon_html_preview()` zeichnet `## Amazon-Beschreibung (Vorschau)` mit Quote-Block (>) der `headline` als bold, `lead` als plain text, bis zu 2 Bullets — exakt wie der Kindle-Shopper die ersten Zeilen über dem "Mehr lesen"-Link liest, ohne HTML-Tags und mit normalisiertem Whitespace. Meta-Zeile mit `Gesamt-Zeichen` + `Keyword-Score` wird nur ausgegeben wenn mindestens einer der beiden > 0 ist (sonst Skip). Filelink auf `amazon_description.html` für die volle Fassung. `render_beginner_summary` nimmt jetzt `amazon_html_preview: dict | None`, Section wird nach `_render_top_kdp_keywords` ausgegeben — direkt unter dem Keyword-Block, wo der Autor das Listing-Targeting zusammen sieht. modules/pipeline.py reorder: `build_amazon_description_html(project)` läuft jetzt VOR `render_beginner_summary`, das vorab berechnete Snippet wird sowohl für die Preview als auch für die nachfolgende `amazon_description.html`/`.md`/`.json`-Persistenz wiederverwendet — keine doppelte Berechnung. 13 neue Unit-Tests: 7 in tests/test_pipeline_helpers.py (None-Input, alle Felder leer → None, headline+lead carried, Bullets-Cap auf 2, leere Bullets übersprungen, Payload bei nur Bullets, Bullets als Tuple) + 6 in tests/test_industrial.py (Section sichtbar mit allen Feldern + Filelink, fehlt bei None/empty, skipt bei whitespace+leeren Bullets, funktioniert mit nur Bullets, omits Meta-Zeile bei char_count=0+keyword_score=0). 479 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Amazon-Description-HTML mit LLM-Pass:** Optional eine LLM-Variante, die statt Template-Bullets echte Highlight-Bullets aus dem Manuskript zieht — schaltbar via AppConfig. *(modules/amazon_html.py: neue `LLM_BULLETS_SYSTEM_PROMPT`, `build_llm_bullets_user_prompt()`, `_parse_llm_bullets_payload()`, `extract_amazon_bullets_via_llm()`; `build_amazon_description_html()` akzeptiert jetzt optional `llm_bullets`. `_select_bullets()` bevorzugt LLM-Bullets, wenn ≥`MIN_BULLETS` brauchbare Items vorliegen, und faellt sonst sauber auf die deterministische Template-Pfad zurueck. Neue Helfer-Funktion `_normalize_bullet_candidates()` clippt auf `BULLET_MAX_CHARS`, dedupliziert case-insensitive und droppt Items unter `LLM_BULLETS_MIN_CHARS`. AppConfig-Flag `amazon_html_llm_bullets_enabled: bool = False` mit YAML-Loader. Pipeline-Helper `PublisherPipeline._maybe_extract_amazon_llm_bullets()` gated den Aufruf: ist der Schalter aus ODER kein `ANTHROPIC_API_KEY` gesetzt, wird der Pfad uebersprungen (geloggt). Jede Exception aus dem LLM-Aufruf wird zu einem geloggten Warning, die Template-Bullets uebernehmen — kein abgebrochener Run. `extract_amazon_bullets_via_llm` schluckt selbst alle inneren Exceptions zu `[]`. Chapter-Titel werden auf `LLM_BULLETS_MAX_CHAPTER_TITLES=20` gedeckelt, damit der Prompt billig bleibt. 16 neue Unit-Tests in tests/test_amazon_html.py + 5 Pipeline-Wiring-Tests in tests/test_pipeline_amazon_html_llm.py decken Happy-Path, Toggle-Aus, fehlender API-Key, LLM-Crash, leere LLM-Antwort, Dedup, Clipping, Min-Length-Filter, Non-String-Items, Prompt-Inhalt, Chapter-Title-Cap und JSON-Payload-Robustness ab. 795 Tests insgesamt gruen.)*
- [ ] **KDP-Keywords mit LLM-Pass:** Aktuelle Slots sind Template-basiert. Optional eine LLM-Variante, die echte Long-Tail-Suchphrasen aus dem Manuskript ableitet — schaltbar via AppConfig.
- [x] **KDP-Keywords-Top-3 in beginner_summary:** Die 3 stärksten Slots (z.B. subject_audience + audience_format + anchor_pair) direkt in beginner_summary.md als Copy-Paste-Block aufnehmen. *(modules/industrial.py: neuer Sub-Renderer `_render_top_kdp_keywords` zeichnet `## KDP-Keywords (Top-3)` mit nummerierten Backticks (copy-paste-fertig), Zeichen-Counter `(Zeichen: N/50)` und `Warum:`-Begründung pro Slot. `render_beginner_summary` nimmt jetzt zusätzlich optional `top_kdp_keywords: list[dict] | None`. Sektion wird komplett weggelassen, wenn keine Daten vorliegen oder die Liste leer ist; Slots mit leerem `text` werden übersprungen (kein Backtick-only Eintrag). modules/pipeline.py: neuer pure helper `_top_kdp_keywords_payload(keywords, *, limit=3)` bevorzugt source-diversity — der erste Keyword pro Source-Familie (subject_format, subject_audience, audience_format, anchor_pair, fallback) wird vor Duplikaten ausgewählt, damit der Autor einen Spread von Suchintents sieht statt drei subject_format-Varianten. Fällt auf Ordnungs-Reihenfolge zurück, wenn diversity das Limit nicht füllt. Tracked picked_texts gegen identische Strings (gleicher Text in zwei Sources erscheint nur einmal). Pipeline reordered: `build_kdp_keywords(project)` wird jetzt VOR `render_beginner_summary` aufgerufen und der vorab berechnete `kdp_keywords` an die nachfolgende `kdp_keywords.md/.json`-Persistenz weitergereicht — keine doppelte Berechnung. 11 neue Unit-Tests: 4 in tests/test_industrial.py (Sektion sichtbar mit allen Feldern + Filelink, fehlt bei None/empty, robust gegen partial dicts ohne rationale, skipt leere Text-Entries), 7 in tests/test_pipeline_helpers.py (None/empty → None, limit=0 → [], source-diversity-Pick statt erste-drei-aus-derselben-Familie, Fallback auf Ordnungs-Reihenfolge wenn nur eine Source-Familie, Clamping auf verfügbare Keywords, Renderer-Felder vollständig, Immutability, Text-Dedup über Sources hinweg). 290 Tests grün, kein bestehender Test gebrochen.)
- [x] **KDP-Keywords-Konflikt-Check:** Warnen, wenn ein Keyword-Slot mit der gewählten KDP-Kategorie überlappt (Amazon ignoriert solche Slots). *(modules/kdp_keywords.py: neuer `KeywordConflict` (frozen dataclass) mit `keyword_text`, `category` und `shared_tokens` (sortiert, immutable Tuple). Neuer pure helper `extract_kdp_categories(project)` liest alle `.md`/`.txt`-Files in `project.metadata_files + project.notes_files`, scrapt Sektionen mit Headern wie `## Kategorie(n)?`, `## KDP[-\s]+Kategorie(n)?`, `## Categor(y|ies)` (case-insensitive via `_CATEGORY_HEADER_RE`), stripped List-Prefixes (`-`, `*`, `1.`, `>`, `•`) via `_LIST_PREFIX_RE`, dedupliziert über Files hinweg. Tolerant gegen fehlende Files, OSError, UnicodeDecodeError. Neuer pure helper `find_keyword_conflicts(keywords, categories)` tokenisiert beide Seiten mit `_normalize_phrase` (ascii-folded, lowercased, alpha-numeric only), schneidet den Token-Set, zieht die `KEYWORD_CONFLICT_STOP_TOKENS` ab (generic nonfiction filler wie "buch", "ratgeber", "praxis", "leitfaden", "sachbuch", "fuer", "und" usw. — sonst würde "ratgeber praxis anleitung" fälschlich als Konflikt mit "Sachbuch / Ratgeber / Beruf" gemeldet) und meldet 1 Konflikt pro Keyword (first-match-wins in Kategorie-Reihenfolge) wenn mindestens `KEYWORD_CONFLICT_MIN_SHARED=1` substantive Token überlappt. Render-Section `## Konflikt-Check (Kategorie vs. Keyword)` via neuem helper `_render_conflict_section()` mit drei States: (a) keine Kategorien deklariert → freundlicher Hinweis mit Beispiel-Markdown-Snippet, der dem Autor zeigt wie er Kategorien in metadata.md hinterlegt — der Konflikt-Check läuft dann automatisch in der nächsten Runde; (b) Kategorien deklariert + keine Konflikte → 🟢-Block "Kein Konflikt"; (c) Kategorien deklariert + Konflikte → 🔴-Block mit Kategorie-Liste, Konflikt-Tabelle `| Keyword | Kategorie | Überlappende Begriffe |` und Empfehlungs-Zeile (Long-Tail-Phrasen mit zusätzlichen Begriffen). `render_kdp_keywords_report_markdown` nimmt jetzt optional `categories` und `conflicts`; bei `categories=None` werden sie aus dem Project gelesen, bei `conflicts=None` aus der Kombination berechnet — Default-Pfad funktioniert ohne Aufrufer-Änderung. modules/pipeline.py: ruft `extract_kdp_categories` + `find_keyword_conflicts` neben `build_kdp_keywords` auf und reicht beide an Render + JSON weiter; JSON-Output trägt jetzt `keywords`, `categories` und `conflicts` Subkeys. 18 neue Unit-Tests in tests/test_kdp_keywords.py decken: empty-keywords/-categories-Pfade, Subject-Overlap als Konflikt, Stop-Tokens (ratgeber) NICHT als Konflikt, 1-Konflikt-pro-Keyword (first-match-wins), sortierte shared_tokens, empty-keyword-text Defensive, no-metadata-files → leere Kategorien-Liste, Parser für `## KDP Kategorien` / `## Kategorie` (singular) / List-Präfixe (1./*/>) / Dedupe über Files hinweg, Render mit Konflikten (Tabelle + 🔴 + Empfehlung), Render bei sauberem State (🟢 + "Kein Konflikt"), Render bei fehlenden Kategorien (Hinweis + Beispiel-Snippet), JSON-Roundtrip. 443 Tests grün, kein bestehender Test gebrochen.)*
- [x] **KDP-Keywords-Top-Limit konfigurierbar:** Aktuell hart auf 3 in `_top_kdp_keywords_payload` — in AppConfig auslagern (`beginner_summary_kdp_keyword_limit: int = 3`), damit Autoren mehr (oder weniger) Slots direkt in der Summary sehen können. *(modules/config.py: neues `AppConfig.beginner_summary_kdp_keyword_limit: int = 3` Feld. `load_config` liest den YAML-Key und clampt auf [1, 7] über `max(1, min(7, int(...)))` — Werte <1 würden den stärksten Slot unterdrücken und so den wichtigsten KDP-Backend-Block aus dem beginner_summary entfernen, Werte >7 würden Slots für die Amazon-API erfinden, die das System gar nicht akzeptiert; beide Pathologien hart abgeschnitten. config.yaml: neuer Key mit Erklär-Kommentar (Default 3, 5-7 für Autoren mit komplexem Keyword-Setup). modules/pipeline.py: neue Konstante `TOP_KDP_KEYWORD_MAX_LIMIT = 7` als Single-Source-of-Truth (Config-Layer und Helper-Layer ziehen aus derselben Konstante). `_top_kdp_keywords_payload` clampt jetzt zusätzlich defensiv auf `[0, TOP_KDP_KEYWORD_MAX_LIMIT]` (limit=0 verhalten bleibt erhalten → `[]` für explizite Stummschaltung). `run_qa` reicht jetzt `self.config.beginner_summary_kdp_keyword_limit` durch. modules/industrial.py: `_render_top_kdp_keywords` zeichnet Heading + Intro jetzt dynamisch aus der tatsächlich sichtbaren Keyword-Anzahl — `## KDP-Keywords (Top-N)` und `Die {N-Word} stärksten Slots` (z.B. „Die fünf stärksten Slots", „Die sieben stärksten Slots"), Singular-Form korrekt bei N=1 („Die stärkste Slot"). `_KDP_KEYWORD_COUNT_WORDS`-Dict als Single Source of Truth für die deutschen Zahlworte 1-7. Whitespace-only `text`-Einträge werden VOR Heading-Berechnung gefiltert, damit der Heading-Count nicht lügt. 6 neue Unit-Tests: 4 in tests/test_config.py (Default 3, YAML-Lesen, Clamp >7 → 7, Clamp <1 → 1), 3 in tests/test_pipeline_helpers.py (out-of-range 99 → 7, limit=1 funktioniert mit Diversity-Pick, limit=7 returnt alle 7 Keywords), 1 in tests/test_industrial.py (heading_scales_with_count: Top-5/Top-7/Top-1 inkl. korrekter deutscher Wortform). 2 Bestandstests in tests/test_industrial.py aktualisiert (`handles_partial_dicts` + `skips_empty_text_entries` erwarten jetzt korrekt „Top-1" statt „Top-3", da sie nur 1 sichtbaren Keyword übergeben). 640 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Sample-Scan-Top-Risiko in beginner_summary:** Schwächsten Sample-Abschnitt (höchstes Abbruch-Risiko) inkl. Fix-Zeile direkt in beginner_summary.md zeigen, damit der Autor sofort weiss, wo der Kindle-Leser bricht. *(modules/industrial.py: neuer Sub-Renderer `_render_weakest_sample` zeichnet `## Schwächster Sample-Abschnitt` (Badge + Score + Risiko-Label + Fix-Zeile), `render_beginner_summary` nimmt jetzt zusätzlich optional `weakest_sample: dict | None`. Sektion wird komplett weggelassen, wenn keine Daten oder kein Risiko vorliegt — keine Kosmetik im Summary, wenn das Sample sauber ist. modules/pipeline.py: neuer pure helper `_weakest_sample_payload(sample_json)` extrahiert die schwächste Section aus `sample_scan.json` und gibt `None` zurück, wenn alle Abschnitte READY sind (kein Hinweis, wenn nichts zu fixen ist). `run_qa` reordered: sample_scan wird jetzt VOR beginner_summary berechnet, damit das Top-Risiko in die Summary fließt — sample_scan-Artefakte werden weiterhin geschrieben, doppelte DOCX-Reads vermieden (cached `sample_scan` + `sample_json`). 9 neue Unit-Tests: 3 in tests/test_industrial.py (Sektion sichtbar, fehlt bei None/empty, robust gegen partial dicts) + 6 in tests/test_pipeline_helpers.py (None-Report, leere Sections, alle READY → None, lowest-scoring Pick, partial dicts, fehlender Status). 220 Tests grün.)*
- [x] **Sample-Scan mit LLM-Pass:** Aktuelle Bewertung ist heuristisch (Regex-Marker). Optional eine LLM-Variante, die jedes Sample-Stück liest und einen Rewrite des Eröffnungssatzes vorschlägt — schaltbar via AppConfig. *(modules/config.py: neues `AppConfig.sample_scan_llm_rewrites_enabled: bool = False` Feld + Loader-Pfad in `load_config`; config.yaml dokumentiert mit Erklär-Kommentar (Off-by-Default, jeder LLM-Fehler fällt stillschweigend auf den heuristischen Pfad zurück). modules/sample_scan.py: `SampleSectionScore` um optionales `opening_rewrite: str = ""` Feld erweitert (frozen, default leer); `to_json()` lässt das Feld weg wenn leer — keine Kosmetik im JSON bei Off-Path. Neue Bausteine: `LLM_REWRITES_SYSTEM_PROMPT`, `LLM_REWRITES_MAX_SECTIONS=6` (Cost-Cap), `LLM_REWRITES_BODY_PREVIEW_CHARS=360` (Prompt-Bound), `LLM_REWRITES_MIN_OPENING_CHARS=12`/`LLM_REWRITES_MAX_OPENING_CHARS=200` (Quality-Gate). Pure helper `build_sample_rewrites_user_prompt(report, section_bodies, *, limit, preview_chars)` listet NUR risky Sections (status REVIEW/FIX) mit Label + Score + schwächster Dimension + Fix-Hinweis + Body-Preview, sortiert nach overall ascending (schlechtester zuerst, cost-bewusst); leerer String wenn nichts zu rewriten ist → Caller short-circuited den LLM-Call. `_parse_sample_rewrites_payload(payload)` extrahiert `{index: opening}` aus dem LLM-JSON, tolerant gegen Shape-Drift (Non-Dict, fehlende `rewrites`, non-int indices, non-string openings), filtert Ausrufezeichen + zu kurze Strings, kappt zu lange via Ellipsis. `extract_sample_rewrites_via_llm(report, section_bodies, llm_completer)` ist die High-Level-Funktion: schluckt Exceptions zu leerem Dict (kein abgebrochener Lauf), short-circuited bei leerem Prompt. Pure helper `apply_sample_rewrites(report, rewrites)` baut ein neues immutables `SampleScanReport` via `dataclasses.replace` — Original wird nie mutiert, gibt sogar den Original-Report zurück wenn das Mapping leer ist (vermeidet Allokation) oder kein Eintrag tatsächlich etwas ändert. Pure helper `section_bodies_from_paragraphs(paragraphs, *, config)` ruft `extract_sample_sections` und liefert das `{index: body}`-Mapping passend zur Report-Index-Achse — kein Off-by-one-Risiko zwischen Prompt und Apply. `render_sample_scan_markdown` zeichnet die Rewrite-Zeile (`- Vorschlag Eroeffnungssatz: _..._`) nur wenn `opening_rewrite` gesetzt ist — kein leeres Feld bei deaktiviertem LLM-Pass. modules/pipeline.py: neuer Pipeline-Helper `_maybe_apply_sample_llm_rewrites(project, sample_scan)` enthält die vier Gates (toggle aus, kein API-Key, keine risky Section, kein Manuskript) und gibt das Sample-Report immer zurück — Pipeline kann den Aufruf bedingungslos in den happy path stellen. `run_qa` ruft den Helper VOR `sample_scan.to_json()` auf, damit die Rewrites in `sample_scan.json` UND `sample_scan.md` landen. Exception aus dem DOCX-Stream oder dem LLM-Call wird geloggt (`sample_scan_llm_rewrites_failed` mit `stage`-Tag) und führt zu Pass-Through ohne Crash. 26 neue Unit-Tests in tests/test_sample_scan_llm.py decken: Default-Rewrite leer, `to_json` lässt leere Rewrites aus, Rewrite in JSON sobald gesetzt, `section_bodies_from_paragraphs`-Indices matchen Report-Indices (kein Off-by-one), Prompt listet nur risky Sections + leer ohne risky Sections + respektiert Limit, Payload-Parser akzeptiert valide Einträge + dropt Ausrufezeichen/zu-kurz + clippt zu-lang + tolerant gegen Garbage, `apply_sample_rewrites` returnt identische Instanz bei empty/whitespace + erzeugt immutable neue Sections bei valid Input (Original-Report unverändert), Markdown zeigt Rewrite + lässt es weg bei leerem Wert, `extract_sample_rewrites_via_llm` short-circuited ohne LLM-Call wenn keine risky Section + swallowed Exceptions + liefert valid Mapping, `load_config` defaultet auf `False` + liest `true`-Toggle, Pipeline-Helper returnt identischen Report bei Off-Toggle/no-Key/no-risky/no-Manuskript ohne LLM-Call, Konstanten haben sane Bounds (Cost-Guard-Regression-Schutz). 889 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Sample-Ratio Konfig:** SAMPLE_RATIO (10%) und MAX_SECTIONS in AppConfig auslagern, damit Autoren mit kurzen Sachbüchern den Scan-Umfang erweitern können. *(modules/sample_scan.py: neue frozen-dataclass `SampleScanConfig` als single source of truth für die fünf bisher hartcodierten Konstanten (`sample_ratio`, `max_ratio`, `max_sections`, `section_target_words`, `min_section_words`). Defaults matchen 1:1 die alten Modul-Konstanten — Bestandstests laufen ohne Änderung. `DEFAULT_SAMPLE_SCAN_CONFIG`-Singleton bekommt jeder Helper als Default-Argument, sodass `extract_sample_sections`/`_bucket_into_sections`/`build_sample_scan_report_from_paragraphs`/`build_sample_scan_report` jetzt ein optionales `config`-Kwarg nehmen. Bestehende Module-Funktionen-Kwargs `sample_ratio` und `max_ratio` bleiben für Backwards-Compat erhalten und überschreiben die Config-Werte, wenn explizit gesetzt. Neuer pure helper `sample_scan_config_from_app(app_config)` baut den `SampleScanConfig` aus den fünf neuen AppConfig-Feldern — single wiring point, kein duplizierter Lese-Code in Pipeline und Tests. modules/config.py: AppConfig um fünf Felder erweitert (`sample_scan_ratio: float = 0.10`, `sample_scan_max_ratio: float = 0.14`, `sample_scan_max_sections: int = 8`, `sample_scan_section_target_words: int = 350`, `sample_scan_min_section_words: int = 90`); `load_config()` liest sie aus YAML mit aggressivem Clamping (`_clamp_float` für die Ratios mit [0.01, 1.0]-Bounds, int-Floors für die Section-Counts so dass `0` nicht zu Endlos-Schleifen führt). Hilfs-Funktion `_clamp_float` neu in config.py — generisch, koerziert ungültige Typen sauber auf den Low-Bound statt zu crashen. config.yaml: fünf neue Keys mit Kommentar dokumentiert (Hinweis für Autoren mit <25k-Wörter-Sachbüchern, `ratio≈0.20` + `min_section_words≈50` zu setzen). modules/pipeline.py: `run_qa` baut den Config jetzt über `sample_scan_config_from_app(self.config)` und reicht ihn an `build_sample_scan_report(project, config=...)` — wirkt automatisch, ohne weitere Caller-Änderungen. 12 neue Unit-Tests in tests/test_sample_scan.py decken: Default-Config matched Legacy-Konstanten (Drift-Schutz), SampleScanConfig ist frozen, höhere `sample_ratio` produziert nachweislich mehr Sample-Wörter bei einem 720-Wort-Buch, `max_sections=3` capped section_count, niedrigere `min_section_words` verhindert das Merging kurzer Sektionen (mehr Sektionen sichtbar), `section_target_words` ist verdrahtet (kein Crash, ≥1 Section), Legacy-Kwargs `sample_ratio`/`max_ratio` überschreiben weiterhin die Config (Backwards-Compat), `sample_scan_config_from_app` liest die fünf AppConfig-Felder verbatim, `load_config` liest die fünf YAML-Keys, `load_config` defaultet auf die alten Werte wenn der Block fehlt, `load_config` clampt invalide Werte (`-0.5`→0.01, `5.0`→1.0, `0`→Min-Floor). 585 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Round-Delta in beginner_summary:** Top-Highlight (z.B. "3 Fixes erledigt, Score +15") direkt in beginner_summary.md aufnehmen, damit der Autor in der zweiten Runde sofort den Fortschritt sieht. *(Duplikat — bereits umgesetzt unter „Round-Delta-Highlight in beginner_summary" weiter unten. `_round_delta_payload` + `_render_round_delta_highlight` in modules/pipeline.py + modules/industrial.py, integriert in run_qa.)*
- [ ] **Round-Delta mit LLM-Pass:** Optional eine LLM-Variante die persistente Fixes interpretiert ("warum bleibt dieser Fix offen — fehlt Zeit, Verständnis oder Material?") — schaltbar via AppConfig.
- [x] **LLM-Fallback Metrik in beginner_summary:** Wenn in einem Lauf das Fallback-Modell verwendet wurde, eine kurze Notiz in beginner_summary.md aufnehmen ("⚠️ Primärmodell `claude-sonnet-4-6` war nicht erreichbar — Bewertung kam von `claude-haiku-4-5-20251001`"), damit der Autor die niedrigere Tiefe einordnen kann. *(modules/llm.py: `LLMClient` trackt jetzt `_primary_calls`, `_fallback_calls`, `_last_primary_model` und `_last_fallback_model` als Instance-State. `complete()` inkrementiert die Zähler nur bei erfolgreichem Call (nicht bei Exception), `_last_primary_model` wird bei jedem Aufruf gesetzt damit das Summary auch im reinen Fallback-Pfad weiß welches Primär-Modell versuchte; `_last_fallback_model` nur bei erfolgreichem Fallback-Call. Neuer Public-Helper `fallback_summary() -> dict | None` liefert `None` wenn nie ein Fallback erfolgreich war (silent-success-Pfad — die Summary bleibt sauber) und sonst ein immutables Dict mit `fallback_used`, `primary_model`, `fallback_model`, `primary_calls`, `fallback_calls`, `total_calls`. modules/industrial.py: neuer Sub-Renderer `_render_llm_fallback_notice(llm_fallback)` zeichnet eine `## ⚠️ Modell-Fallback aktiv`-Sektion mit Erklärung (geringere Tiefe), Modell-Zeilen (`**Primär:** ... (fehlgeschlagen)` / `**Fallback:** ... (eingesprungen)`), Aufruf-Zähler und Empfehlung zur Wiederholungsrunde. Sektion wird komplett weggelassen wenn: payload None/empty, `fallback_used` falsy, oder `fallback_calls<=0` — keine kosmetische Warnung wenn nichts passiert ist. `**Primär:**`-Zeile wird übersprungen wenn `primary_model` fehlt — partial-payload-tolerant. `render_beginner_summary` nimmt jetzt zusätzlich `llm_fallback: dict | None`, Sektion wird VOR `_render_round_delta_highlight` ausgegeben damit der Autor die Warnung sofort sieht. modules/pipeline.py: ruft `self.llm.fallback_summary()` auf und reicht das Ergebnis an `render_beginner_summary` weiter — wirkt automatisch in `--full`-Runden ohne weitere Änderungen, hat keinen Effekt im Schnellrunden-Pfad (qa ohne API-Key) weil dort keine LLM-Calls passieren. 10 neue Unit-Tests: 5 in tests/test_llm.py (Summary None bei keinen Calls, Summary None bei nur Primär-Erfolg, Summary mit Modellen+Zählern nach Fallback, Summary mit Mixed Primary/Fallback-Calls, Summary None wenn beide Modelle fehlschlagen — kein Notice für komplett gescheiterte Runde) + 5 in tests/test_industrial.py (Sektion sichtbar mit allen Feldern + Modell-Empfehlung, fehlt bei None/empty, fehlt bei `fallback_used=False`, fehlt bei `fallback_calls=0` auch wenn used=True, partial payload ohne primary_model lässt nur die Primär-Zeile weg). 453 Tests grün, kein bestehender Test gebrochen.)*
- [x] **LLM-Retry mit Backoff vor Fallback:** Aktuell schaltet `complete()` bei der ersten Exception sofort auf das Fallback-Modell um. Transientes Rate-Limit oder Timeout könnte vor dem Modellwechsel 1× retryt werden (exponential backoff, max 2 Versuche pro Modell), bevor der Fallback einspringt. *(modules/llm.py: neuer pure helper `_call_with_retries(model, system, user, *, label)` als gemeinsamer Aufrufpfad für Primär- und Fallback-Modell. Pro Modell `AppConfig.llm_retry_attempts` Versuche (production-Default 2, test-Default 1 für Backwards-Compat) mit exponentiellem Backoff `llm_retry_backoff_seconds * 2**(attempt-1)` zwischen den Versuchen (0,5s → 1s → 2s). `_sleep` als überschreibbares Class-Attribute (default `time.sleep`); Tests setzen es per Instance-Override auf `lambda _: None` und verzichten auf reale Wall-Clock-Zeit. `ConfigError` wird NICHT retryt — "anthropic nicht installiert" und ähnliche Hard-Errors propagieren sofort, kein nutzloser Retry-Storm; in `complete()` auch nicht zum Fallback gereicht (würde am gleichen Import scheitern). Neuer Log-Event `model_call_retry` mit `model`, `next_attempt`, `wait_seconds`, `label` — Test-Inspektoren und beginner_summary-Traces können Primär- vs. Fallback-Retries unterscheiden. `model_call_error`-Event trägt jetzt zusätzlich `attempt`, `max_attempts` und `label`, damit der Verlauf einer transienten Runde im JSONL-Log nachvollziehbar bleibt. modules/config.py: `AppConfig` um `llm_retry_attempts: int = 1` und `llm_retry_backoff_seconds: float = 0.5` erweitert (frozen-dataclass, backwards-kompatibel — bestehende Test-Helper, die AppConfig kwargs-basiert konstruieren, behalten ihre Semantik). `load_config()` liest `llm_retry_attempts: 2` und `llm_retry_backoff_seconds: 0.5` aus `config.yaml` als Production-Default; negative Werte werden auf 1 bzw. 0.0 geclamped, damit ein `-3` im YAML keinen Zero-Loop produziert. config.yaml um beide Keys + Kommentar erweitert. tests/test_llm.py: `_build_client` nimmt jetzt `retry_attempts`/`retry_backoff_seconds` kwargs (Default 1 / 0.0 = no-retry-Modus für Bestandstests), patcht `_sleep` automatisch auf no-op. 11 neue Unit-Tests: Erfolgreicher Retry auf 2. Primär-Versuch ohne Fallback (transient rate-limit Szenario), Primär-Versuche werden ausgeschöpft bevor Fallback einspringt (Calls `[PRIMARY, PRIMARY, FALLBACK]`), Fallback-Modell wird ebenfalls retryt (Labels `{primary, fallback}` in retry-Events), exponentielles Backoff sleept `[0.5, 1.0, 0.5, 1.0]` bei attempts=3, kein Sleep bei backoff_seconds=0, ConfigError wird nicht retryt (genau 1 Call), Log-Event trägt attempt/max_attempts/label/next_attempt/wait_seconds, attempts<=0 wird auf 1 geclamped, load_config liest YAML-Werte korrekt, load_config defaultet auf attempts=2 + backoff=0.5, load_config clampt negative Werte auf 1/0.0. 12 Bestandstests in test_llm.py weiterhin grün — explizite `retry_attempts=1` im Helper konserviert die alte 1-Versuch-Semantik. 574 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Customer-Journey + Cross-Selling-Beispielbuch:** Echtes Buch `Unter Fünfzig Euro` als versendetes Beispielbuch in `release/beispielbuch/` (DOCX + metadata.md mit KDP-Tags + LIES_MICH.txt + Amazon-Link). End-to-End-Test `tests/test_customer_journey.py` verifiziert den exakten Pfad „Kunde entpackt ZIP → wählt beispielbuch → klickt Schnellrunde → bekommt lesbare beginner_summary.md ohne API-Key". Test deckte zwei Bugs auf, die mitgefixt wurden: (a) `QUICK_ROUND_ARTIFACTS` enthielt KDP-Keywords/Amazon-HTML/Sample-Scan/Rewrites nicht → der Kunde sah diese Assets nach Klick auf „Reportordner öffnen" nicht; (b) Discovery überschrieb explizite `## KDP Titel`-Marker mit dem ersten Wort einer alphabetisch früheren `.txt` → Explicit-Override-Logik in `_extract_metadata_from_text` + `_merge` Helper. README ergänzt um „Was der Kunde nach dem Download sieht".

## Neu entdeckt — Customer-Journey-Folgearbeit

- [x] **PyInstaller-Build im CI verifizieren:** Aktuell baut `build_exe.bat` lokal eine `dist/BookPublisher.exe`, aber kein automatisierter Smoke-Test prüft, ob die EXE auf einer frischen Windows-Box ohne Python startet. Github-Actions-Job mit `windows-latest` der die EXE baut und auf das mitgelieferte `release/beispielbuch/` losschickt → Artefakt-Check identisch zu `test_customer_journey.py`. *(modules/cli.py: neuer `smoke`-Subcommand kurz-schließt VOR der Pipeline-Konstruktion (kein SkillRegistry, kein AgentMemory) und gibt eine Status-Zeile mit project_root + default_model + fallback_model + `config_loaded: yes` aus. Reaching this line ist DER Smoke-Beweis: argparse + load_config + jeder transitiv importierte Modul (LLMClient, Pipeline, readers, scoring …) hat funktioniert — genau das, was bei einem kaputten PyInstaller-Bundle als erstes fehlschlägt. parser.choices um `smoke` erweitert. scripts/build_windows_cli.bat: neuer Build-Script für die Konsolen-Variante des EXE (`--console`, Entry-Point `main.py`, Output `dist/BookPublisher-cli/BookPublisher-cli.exe`) — die windowed GUI-Variante schluckt stdout und ist im CI nicht testbar, die Console-Variante hat echte stdout/stderr-Pipes. .github/workflows/ci.yml: neuer Job `exe-build` auf `windows-latest`, läuft parallel zum bestehenden `test`-Job. Schritte: (1) Python 3.12 + requirements-dev.txt (inkl. pyinstaller), (2) Build CLI-EXE via build_windows_cli.bat, (3) Build GUI-EXE via build_windows_app.bat, (4) Verify beide Dateien existieren und sind >5 MB (PyInstaller-bundled Python ist ~10 MB+, alles drunter wäre ein leerer Stub-Bug), (5) Smoke-Test der CLI-EXE: `BookPublisher-cli.exe smoke` muss Exit 0 und String "smoke test OK" liefern, (6) End-to-End-Smoke-Test gegen `release/beispielbuch`: `BookPublisher-cli.exe qa --input-path release/beispielbuch` muss Exit 0 liefern und ein `artifacts/beginner_summary.md` mit >200 Zeichen schreiben — exakt der Customer-Journey-Pfad, jetzt im CI verifiziert, (7) Distribution-ZIP via `python -m modules.release_packager`, (8) Upload aller drei Artefakte (GUI-EXE, CLI-EXE, ZIP) als GitHub-Actions-Artefakt mit 14-Tage-Retention für manuelles Pre-Release-Testing. README.md: Build-Sektion um beide neuen Skripte erweitert (`build_windows_cli.bat`, `build_release_zip.bat`) plus expliziter Hinweis dass das CI-Job-Setup einen kaputten EXE auf jedem Push fängt bevor Kunden ihn sehen. 4 neue Unit-Tests in tests/test_cli.py: smoke-Command Exit 0 mit erwartetem Output, smoke-Command propagiert ConfigError als Exit 2, smoke-Command konstruiert KEINE PublisherPipeline (das ist der Witz — kein heavy init), smoke-Command ist in den parser.choices gelistet. `test_parser_accepts_all_documented_commands` um `"smoke"` erweitert — Drift-Schutz wenn jemand die Choice-Liste später kürzt. Live-Smoke gegen Local-Python: `python main.py smoke` returnt 0 und druckt die erwartete Status-Zeile. 618 Tests grün, kein bestehender Test gebrochen. Verbleibender Bind: Der erste CI-Run nach dem Push wird die echte Verifikation liefern — wenn dort die EXE doch fehlschlägt, sehen wir es VOR dem nächsten Customer-Release.)*
- [x] **Release-ZIP-Builder-Skript:** `scripts/build_release_zip.bat` der `dist/BookPublisher.exe` + `release/beispielbuch/` + `BookPublisher starten.bat` + Kurz-LIES_MICH.txt in ein einzelnes `BookPublisher.zip` packt, das auf die Homepage hochgeladen werden kann. *(modules/release_packager.py: neues Modul mit `build_release_zip(project_root, output_path, *, release_dir=None, exe_path=None, launcher_path=None, top_readme_text=...)` als pure-Python-Entry-Point — keine Drittabhängigkeit, baut den ZIP per `zipfile.ZipFile` deterministisch (sortierte rglob-Iteration → byte-identische ZIPs zwischen Runs für Checksum-Verifikation). Vier Komponenten landen drin: (1) `beispielbuch/`-Tree rekursiv mit Forward-Slash-Arcnames (RFC-konform), (2) `BookPublisher starten.bat` für Python-Nutzer ohne EXE, (3) PyInstaller-EXE wenn vorhanden (`dist/BookPublisher/BookPublisher.exe`-Default-Pfad, override via `--exe`-Flag), (4) Top-Level `LIES_MICH.txt` inline-generiert (4-Schritt-Anleitung Doppelklick→Ordner→Pruefrunde→beginner_summary). Fehlende Komponenten werden über `warnings`-Liste in `ReleaseManifest` zurückgegeben statt zu crashen — lokale Builds ohne PyInstaller produzieren weiterhin ein Source-only-ZIP, der Customer-Journey-Test-Path funktioniert. Output-Parent wird automatisch erzeugt (`mkdir(parents=True, exist_ok=True)`), existierende ZIPs werden überschrieben (idempotent). Developer-Artefakte (`__pycache__`, `.pyc`, `.DS_Store`, `Thumbs.db`, `.git`, `.pytest_cache`) sind in `EXCLUDED_PATTERNS` und werden automatisch gefiltert. Neue frozen-dataclass `ReleaseManifest` mit `output_path`, `included_files`, `warnings`, `total_bytes` — Drift-Schutz und JSON-fähig für CI-Integration. CLI-Entry-Point: `python -m modules.release_packager [--output ...] [--exe ...] [--release-dir ...]` mit `argparse`, druckt eine zusammengefasste Status-Zeile + Warnungen + Gesamt-Bytes. scripts/build_release_zip.bat: wrappt die Python-CLI für DEV-Convenience (`cd` ins Projekt-Root, Errorlevel-Check). release/README.md: aktualisierte Build-Anleitung (`build_windows_app.bat` für die EXE, `build_release_zip.bat` für den finalen ZIP, der dann auf die Homepage geht). 18 neue Unit-Tests in tests/test_release_packager.py, alle CI-fähig in `tmp_path` ohne Netzwerk: Beispielbuch-Inhalt wird komplett gepackt (4 Dateien), EXE wird gepackt wenn vorhanden, fehlende EXE → Warnung statt Crash, EXE-Pfad existiert nicht → Warnung, Launcher wird gepackt, fehlender Launcher → Warnung, Top-Level LIES_MICH.txt enthält Doppelklick/Pruefrunde/beispielbuch, Output-Parent-Dir wird angelegt, existierender stale ZIP wird überschrieben, `__pycache__`/`.pyc` werden ausgefiltert, `total_bytes` >= Cover-Größe, ReleaseManifest ist frozen, fehlender `beispielbuch/`-Ordner → Warnung statt Crash (ZIP wird trotzdem brauchbar), deterministic order über zwei Builds, `EXCLUDED_PATTERNS`-Drift-Schutz, Custom-Top-Readme-Text wird verbatim geschrieben, CLI-Entry-Point baut den ZIP an `--output`-Pfad (exit 0), CLI returnt 0 auch bei Warnungen. Live-Test gegen das echte Repo: ZIP ist 503KB, enthält 6 Dateien (4 beispielbuch + Launcher + Top-Level-Readme), nur Warnung "Kein EXE-Pfad" wenn `dist/` leer ist — exakt das gewünschte Verhalten für Dev-Maschinen ohne PyInstaller-Build. 614 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Beispielbuch-Cover ergänzen:** `release/beispielbuch/` enthält jetzt das echte KDP-Cover (`cover.jpg`, 1600×2560, 436 KB) aus der Kundenfassung-PDF extrahiert. Discovery erkennt es korrekt — `missing_assets` ist leer. Customer-Journey-Test prüft Existenz, Mindestgröße und Discovery-Detection. Damit sieht der Kunde beim allerersten Test ein vollständiges Buchpaket (Manuskript + Cover + Metadaten) und der Cover-Review-Gate hat reales Material zur Bewertung.
- [ ] **Amazon-Beschreibung-Bullets aus Manuskript:** Aktuelle Bullets in `release/beispielbuch/metadata.md` sind manuell verfasst. LLM-Pass könnte aus den Kapiteln direkt die stärksten Verkaufs-Bullets extrahieren — Beispielbuch dient dann gleichzeitig als Live-Demo dieser Funktion.
- [x] **GUI-Smoketest mit Mock-Tkinter:** Aktueller Customer-Journey-Test prüft die Pipeline, aber nicht den `messagebox.showerror`-Pfad des GUI. Ein Test der `gui._poll_events` mit einer `ManuscriptReadError` als Payload aufruft (Tk-loop gemockt) würde die User-Dialog-Verzweigung absichern. *(gui.py: neue pure helper `error_dialog_payload(exc) -> tuple[str, str]` extrahiert die Dispatch-Logik aus dem inline-if/elif-Block in `_poll_events` — mappt `ManuscriptReadError` → `DIALOG_TITLE_MANUSCRIPT`, `ConfigError` → `DIALOG_TITLE_CONFIG`, alles andere → `DIALOG_TITLE_GENERIC` und liefert `str(exc)` verbatim. Drei Modul-Konstanten `DIALOG_TITLE_*` als single source of truth — Drift-Schutz gegen tippfehlerhafte Title-Strings im Code. Neuer Method `_handle_error_event(self, payload)` extrahiert die komplette Error-Branch aus `_poll_events` (status setzen, busy zurücksetzen, Dialog zeigen, Report-Pane mit derselben Message befüllen). Payload-Typ `object` mit Defensive-Branch für Non-Exception-Inputs (z.B. wenn der Worker einen String statt einer Exception in die Queue legt) — kein Type-Crash für den Endnutzer. `_poll_events` ruft jetzt nur noch `self._handle_error_event(payload)` auf — der inline if/elif-Block wurde komplett entfernt. tests/test_gui_error_dispatch.py: 11 neue Unit-Tests, CI-fähig (kein Tk-Display, kein Filesystem). `_load_gui_module()` reloadet gui.py frisch pro Test damit Monkeypatching auf `messagebox.showerror` deterministisch greift. `_GuiStub` (dataclass mit `_StubVar`, `_set_busy`, `_set_report_text`) mimt die Surface, die `_handle_error_event` aufruft, ohne `tk.Tk.__init__` zu instanziieren (das benötigt ein Display). `_invoke_handle_error` ruft `PublisherGui._handle_error_event(stub, payload)` direkt — in Python 3 ist `Cls.method` die rohe Funktion, kein Descriptor-Binding, daher reicht die direkte Übergabe des Stubs als `self`. `showerror_recorder` fixture patcht `messagebox.showerror` per `monkeypatch` und sammelt alle Aufrufe in einer Liste. Tests decken: alle drei Dispatch-Branches (Manuscript/Config/Generic), Status-Reset auf "Fehler.", `_set_busy(False)`, Report-Pane mirrored die Dialog-Message, str(exc)-Verbatim (kein Traceback-Leak), Defensive-Branch für Non-Exception-Payload (String), exakt ein `showerror`-Call pro Event, Reihenfolge bei mehreren sequenziellen Errors. 532 Tests grün, kein bestehender Test gebrochen.)*
- [x] **CLI-Exit-Codes dokumentieren:** Neuer Exit-Code 3 für `ManuscriptReadError` ist nirgends dokumentiert. README-Abschnitt "Run" um eine Tabelle Exit-Code → Bedeutung ergänzen, damit Power-User die CLI in Scripts einbinden können. *(modules/cli.py: vier benannte Konstanten als Single Source of Truth — `EXIT_SUCCESS=0`, `EXIT_GENERIC_ERROR=1`, `EXIT_CONFIG_ERROR=2`, `EXIT_MANUSCRIPT_ERROR=3`. Alle Literal-Returns in `main()` (Success-Pfad, ConfigError-Catch, ManuscriptReadError-Catch, generischer Exception-Catch, Round-Success-Pfad) auf die Konstanten umgestellt — ein Symbol-Drift macht die Tabelle vs. Code sofort offensichtlich. README.md: neuer `### Exit codes`-Abschnitt unter `## Run` mit (a) Klartext-Erklärung des Power-User-Use-Cases, (b) Tabelle `| Code | Constant | Meaning | Typical cause |` für alle vier Codes, (c) PowerShell-Beispiel das `$LASTEXITCODE` switcht. tests/test_cli.py (neu): 8 Unit-Tests sperren den Kontrakt: Konstanten-Werte (0/1/2/3 müssen genau diese Codes bleiben), pairwise-unique (keine Drift wenn jemand zwei Codes versehentlich gleichsetzt), main() returnt EXIT_CONFIG_ERROR wenn `load_config` ConfigError wirft, main() returnt EXIT_MANUSCRIPT_ERROR wenn die Pipeline ManuscriptReadError wirft (mit gemockter `PublisherPipeline`-Klasse), main() returnt EXIT_GENERIC_ERROR für andere Exceptions (RuntimeError), main() returnt EXIT_SUCCESS bei normalem Scan, `build_parser()` akzeptiert alle dokumentierten Subcommands (scan/qa/round/review/cover/launch/all), README enthält alle vier Konstanten-Namen + Tabellenzeilen (`| 0 |`, `| 1 |`, …) — Drift-Schutz: bricht, wenn die README hinter dem Code zurückbleibt. 552 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Score-Verlauf-Highlight in beginner_summary:** Letzte 2–3 Score-Einträge (z.B. "78 → 82 → 85, Trend steigt") aus score_history.json direkt in beginner_summary.md aufnehmen. *(modules/industrial.py: neuer Sub-Renderer `_render_score_history_highlight` zeichnet `## Score-Verlauf` mit `score_badge`-Emoji pro Eintrag, Delta-Suffix (`+8`/`-15`/`±0`) und einer Trend-Zeile (steigt/sinkt/stabil) mit eigenem Trend-Badge (🟢 bei +N, 🔴 bei -N, 🟡 bei ±0). `TREND_LABELS`-Konstante als einzige Quelle der Trend-Begriffe. `render_beginner_summary` nimmt jetzt zusätzlich optional `score_history_highlight: dict | None`. Sektion wird komplett weggelassen, wenn weniger als 2 Datenpunkte vorliegen (erste Runde) — keine Kosmetik ohne Trend. modules/pipeline.py: neuer pure helper `_score_history_payload(history, *, limit=3)` baut die letzten N Einträge als immutable Liste mit per-entry-Delta auf, clamped `limit` auf min. 2 (1-entry-Fenster wäre sinnlos), liefert `entry_count` (volle History-Größe) zusätzlich zu den Window-Daten. `run_qa` reordered: `load_score_history`/`append_score_history` werden jetzt VOR `render_beginner_summary` aufgerufen, das vorab berechnete `history` an die nachfolgende Persistenz weitergereicht — keine doppelte Append-Logik. 13 neue Unit-Tests: 9 in tests/test_pipeline_helpers.py (None-History, leere Entries, Single-Entry → None, rising/falling/stable Trend, Limit-Cap mit erhaltenem entry_count, Limit<2 clampt auf 2, fehlende Score-Felder, Immutability), 6 in tests/test_industrial.py (Sektion sichtbar mit allen Feldern + READY-Badge bei rising, falling nutzt 🔴-Badge + `-N`-Delta, stable nutzt 🟡-Badge + `±0`-Delta, fehlt bei None/empty, fehlt bei Single-Entry, robust gegen partial entries). 278 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Score-Verlauf Graph als PNG:** Optionaler matplotlib-Pass der score_history.json in artifacts/<project>/score_history.png rendert. Schaltbar via AppConfig (Default aus), damit kein Pflicht-Dependency-Bloat entsteht. *(modules/score_history_graph.py: neues pure-Python-Modul. `ChartSeries` + `ChartDataset` + `ChartRenderResult` (frozen dataclasses) als immutable Render-agnostische Datenstrukturen. `build_chart_dataset(history, *, project_title=None) -> ChartDataset | None` extrahiert pro Runde Timestamp, Industrial-Score und alle vier optionalen Metriken (arc_score/positioning_score/balance_score/readability_score) aus dem score_history-Payload. Returns `None` für: leere History, <2 Einträge (keine Trendlinie). Optionale Series werden nur eingehängt, wenn mindestens eine Runde echte Daten trägt — Nones bleiben als `None` erhalten, damit der Renderer die Linie an der Fehlstelle unterbrechen kann (statt eine irreführende 0 zu zeichnen). Non-dict Entries und nicht-numerische Industrial-Scores werden defensive übersprungen/coerced. `CHART_OPTIONAL_METRICS`-Konstante als Drift-Schutz gegen neue score_history-Felder. `render_history_chart_png(dataset, output_path, *, renderer=None) -> ChartRenderResult` nimmt einen Renderer-Callable (default = lazy-imported matplotlib via `_matplotlib_renderer`) und verpackt jede Exception in eine strukturierte `ChartRenderResult` mit `success: bool`, `output_path` und `message`. ImportError ("matplotlib nicht installiert") wird zu einer aktionablen Message inkl. `pip install matplotlib`. Default-Renderer setzt `matplotlib.use("Agg")` für Headless-Betrieb (CI, Windows-EXE, Server), zeichnet eine Multi-Line-Plot 0–100 mit Datum-Tick-Labels (`YYYY-MM-DD`), Marker pro Runde und 120dpi-Output. modules/config.py: neues `AppConfig.score_history_graph_enabled: bool = False` Feld. `load_config` liest YAML-Key, coerced über `bool(...)` (`"yes"`, `true`, `1` → True). config.yaml: neuer Key dokumentiert (off by default, kein matplotlib-Bloat im EXE). modules/pipeline.py: nach `score_history.md`-Persistenz wird bei `score_history_graph_enabled=True` `build_chart_dataset(history, project_title=project.title)` aufgerufen, `render_history_chart_png(dataset, <project>/score_history.png)` schreibt das PNG; das Ergebnis (success + path + message) wird als `score_history_graph` JSONL-Event geloggt — kein Crash bei fehlender matplotlib-Installation. `score_history.png` in der Mirror-Liste in `run_qa` und in `QUICK_ROUND_ARTIFACTS` (modules/rounds.py) eingetragen, damit das PNG mit `shutil.copy2` (binary-safe) in den Reportordner gespiegelt wird. 20 neue Unit-Tests in tests/test_score_history_graph.py (CI-fähig, kein matplotlib-Install nötig): build_chart_dataset (None/empty/single-entry → None, in-order industrial, project_title verwendet/fallback, optional series omitted/included, None-Werte bleiben erhalten, non-int coerced auf 0, non-dict Einträge übersprungen, frozen-Drift-Schutz, CHART_OPTIONAL_METRICS matched score_history-Felder), render_history_chart_png (None-Dataset → success=False, Fake-Renderer DI mit captured dataset+path, ImportError gibt actionable Message mit `pip install`, generischer Error verpackt), AppConfig (Default off, YAML-Lesen aktiviert, fehlender Key off, YAML-`"yes"` coerced auf True). 774 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Pro-Gate Trend im Score-Verlauf:** Aktuell wird der Aggregat-Score und der Gate-Snapshot pro Runde gespeichert. Eine zusätzliche markdown-Sektion mit "Gate X: 60→75→80" pro Gate würde dem Autor zeigen, welcher Hebel sich bewegt. *(modules/score_history.py: neuer pure helper `build_gate_trends(entries, *, min_points=2)` walked alle Einträge in chronologischer Reihenfolge, gruppiert Scores pro Gate-Key und liefert eine immutable Tupel-Liste mit `name`, `label` (aus `GATE_DISPLAY_LABELS`, Fallback humanized Title-Case), `scores` (tuple, oldest→newest), `first`/`last`/`delta` und `badge` (aus `score_badge` für den letzten Score). Gates mit nur einem Datenpunkt werden übersprungen — die Sektion zeigt nur, was sich bewegt. `min_points<2` wird auf 2 geclamped (single-point hat keinen Trend). Reihenfolge spiegelt die Erst-Erscheinung über alle Entries hinweg, damit die Tabelle zwischen Runden stabil bleibt. Neuer Sub-Renderer `_render_gate_trend_section(trends)` zeichnet eine `## Gate-Verlauf`-Tabelle `| Gate | Verlauf | Delta | Status |` mit Trajektorie `60/100 → 75/100 → 90/100`, Delta-Suffix `+30`/`-15`/`±0` und dem Badge-Emoji für den aktuellsten Stand. Sektion wird komplett weggelassen, wenn keine Gate-Trends ≥2 Punkte vorliegen — kein leerer Header bei der ersten Runde. `render_score_history_markdown` ruft den neuen Renderer nach der Aggregat-Tabelle und vor der Top-Fixes-Liste auf. `from modules.industrial import GATE_DISPLAY_LABELS, score_badge` als einzige neue Abhängigkeit (industrial.py importiert score_history nicht — keine Zyklen). 11 neue Unit-Tests in tests/test_score_history.py: leere Entries, Single-Point-Skip, First-Occurrence-Ordering über zwei Runden mit unterschiedlicher Gate-Reihenfolge, Endpoints + Delta + Scores-Tupel + Badge + Label, negative Delta nutzt 🔴-Badge, unbekannte Gates fallen auf humanized Title-Case zurück, invalide Einträge/Gates (Strings, leerer Name, fehlender Score) werden ignoriert, Markdown enthält `## Gate-Verlauf` + alle Labels + Trajektorie + `+30` + `±0`, Single-Entry-History rendert keine Sektion, `min_points=1` wird auf 2 geclamped. 362 Tests grün, kein bestehender Test gebrochen.)*

## Neu entdeckt — Competitive-Positioning-Folgearbeit

- [x] **Positionierung-Top-Angle in beginner_summary:** Stärkster Differenzierungs-Angle + Positionierungs-Satz direkt in beginner_summary.md aufnehmen, damit der Autor den Pitch sieht, ohne `competitive_positioning.md` separat öffnen zu müssen. *(modules/industrial.py: neuer Sub-Renderer `_render_top_positioning` zeichnet `## Positionierung` mit Nische + Konfidenz, Zielgruppe, stärkstem Angle (Claim + Beleg + Stärke 0–100) inkl. `score_badge`-Emoji und einem copy-paste-fähigen Positionierungs-Satz im Quote-Block. `render_beginner_summary` nimmt jetzt zusätzlich optional `top_positioning: dict | None`. Sektion wird komplett weggelassen, wenn weder Pitch noch Claim Inhalt haben — keine Kosmetik ohne Signal. modules/pipeline.py: neuer pure helper `_top_positioning_payload(positioning_report)` filtert die `kein_signal`-Fallback-Angle aus und liefert ein immutables Dict mit `angle_key/claim/evidence/strength`, `pitch`, `niche_label/confidence` und `audience`. `run_qa` reordered: `build_positioning_report` wird jetzt VOR `render_beginner_summary` aufgerufen, das vorab berechnete Report-Objekt wird an die nachfolgende `competitive_positioning.md/.json`-Persistenz weitergereicht — keine doppelte Berechnung. 11 neue Unit-Tests: 6 in tests/test_pipeline_helpers.py (None-Report, leere Angles, nur `kein_signal` → None, Strength 0 → None, erstes Angle gewinnt, Pitch+Niche-Felder erhalten, Immutability gegen Caller-Mutation) + 5 in tests/test_industrial.py (Sektion sichtbar mit allen Feldern + READY-Badge bei Stärke 80, fehlt bei None/empty, fehlt bei reinem Whitespace-Pitch+leerem Claim, robust gegen partial payload ohne Niche/Evidence, Stärke 70 nutzt 🟡-Badge).)*
- [ ] **Positionierung mit LLM-Pass:** Aktuell ist der Report heuristisch. Optional eine LLM-Variante, die echte Beispiel-Wettbewerbsbücher in derselben Nische benennt (Titel + ASIN + Schwäche) — schaltbar via AppConfig.
- [x] **Nischen-Vokabular pro Buch erweiterbar:** Autor sollte in `metadata.md` einen `## Nischen-Begriffe`-Block hinterlegen können, der die NICHE_TERMS-Erkennung pro Buch ergänzt (z.B. branchenspezifische Akronyme). *(modules/competitive_positioning.py: neuer pure helper `extract_niche_vocab_overrides(project) -> dict[NicheKey, tuple[str, ...]]` liest `.md`/`.txt`-Files aus `project.metadata_files + project.notes_files` und scrapt `## Nischen-Begriffe` / `## Nischen-Vokabular` / `## Niche-Terms` / `## Niche-Vocabulary` Sektionen. Zwei Formate werden akzeptiert: (a) **Inline** — `KI: agentic, llm, ragstack` (Niche-Name + Doppelpunkt + comma-separierte Liste), (b) **Subblock** — `### KI` mit darunter `- agentic`, `- llm`. Niche-Namen werden via `_NICHE_KEY_ALIASES` (50+ Aliases) auf den kanonischen NICHE-Key gemappt: akzeptiert technische Keys (`ki_und_ai`), kurze Labels (`KI`, `Finanzen`, `CFO`, `Mindset`), volle Display-Labels (`KI / Künstliche Intelligenz`, `Finanzen / CFO / Controlling`). Tokens werden via `_split_vocab_value` ascii-folded (Umlaute→ae/oe/ue/ss) und auf min 2 Zeichen gefiltert — passt zur gleichen Shape wie `NICHE_TERMS`. Dedup über First-Occurrence-Order pro Niche. Tolerant gegen fehlende Files (OSError/UnicodeDecodeError) und unbekannte Niche-Namen (silently skipped). `detect_niche(project)` nutzt jetzt die Overrides additiv zu `NICHE_TERMS` — ein domain-spezifischer Token wie "ragstack" oder "ebit" tilt die Detection korrekt zur passenden Niche, ohne dass die Built-in-Vokabularien erweitert werden müssen. Confidence-Berechnung bleibt unverändert (Score-share-of-total-hits). 11 neue Unit-Tests in tests/test_competitive_positioning.py decken: leere Sektion → leeres Dict, Inline-Form-Parsing mit 2 Niches, Subblock-Form mit `###`-Subheadern, technischer Key wird akzeptiert, Umlauts werden ascii-folded (führungskräfte→fuehrungskraefte), unbekannte Niche-Namen werden ignoriert (nur valide bleiben), Dedup über Mischform Inline+Subblock, Tokens <2 Zeichen werden gedroppt, detect_niche nutzt Override-Terms (zero Built-in Hits aber `ragstack` im Description → KI gewinnt), no-usable-overrides ändert Detection nicht, fehlende Files tolerant. 521 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Positionierung-Score in score_history.json:** Eine zusätzliche `positioning_score`-Spalte (Durchschnitt der Top-3-Angle-Strengths) würde dem Autor zeigen, ob seine Metadaten-Iterationen die Positionierung schärfen. *(modules/score_history.py: `ScoreHistoryEntry` um optionale `positioning_score: int | None` und `positioning_delta: int | None` erweitert (default `None`, backwards-kompatibel für bestehende JSON-Files). Neuer pure helper `_previous_optional_field(previous_entries, field_name)` als generischer Ersatz für `_previous_arc_score` — walked die History rückwärts und liefert den letzten gesetzten Wert eines optionalen Feldes; verwendet jetzt sowohl für `arc_score` als auch `positioning_score` als Single Source of Truth für Delta-Vergleiche gegen die letzte Runde mit echten Daten. `append_score_history(...)` nimmt jetzt zusätzlich `positioning_score: int | None = None`; Delta wird gegen den letzten Eintrag mit echtem positioning_score berechnet, Runden ohne Positionierung werden übersprungen. Neuer Render-Helper `_format_optional_score_cell(score, delta)` als gemeinsame Formatierung für Arc + Positionierung — produziert `85/100`, `85/100 (+25)`, `60/100 (-15)`, `72/100 (±0)` oder `-`. `render_score_history_markdown` baut den Tabellenkopf jetzt dynamisch zusammen (header_cells als Liste): Arc- und Positionierung-Spalten werden konditional angefügt, wenn mindestens ein Eintrag den jeweiligen Wert trägt. Bestehende Header-Format-Tests (`| Datum | Runde | Modus | Score | Delta | Decision |` und die Arc-only-Variante) bleiben byte-identisch. modules/pipeline.py: neuer pure helper `_positioning_score(positioning)` bildet den 0–100-Score aus dem `PositioningReport` als gerundeten Mittelwert der Top-3 `unique_angles`-Strengths, filtert die `kein_signal`-Fallback-Angle und 0-strength-Angles, clamped defensiv auf [0,100]. Returns `None`, wenn kein Report oder kein echtes Signal existiert — Score-History trägt dann "keine Positionierung verfügbar" statt einer irreführenden 0. `run_qa` reordered: `build_positioning_report` läuft jetzt VOR `append_score_history`, das vorab berechnete Report-Objekt wird sowohl für die Score-Berechnung als auch für die nachfolgende Persistenz in `competitive_positioning.md/.json` wiederverwendet — keine doppelte Berechnung, kein zusätzlicher DOCX-Read. 19 neue Unit-Tests: 11 in tests/test_score_history.py (Score wird gespeichert mit/ohne previous, Delta überspringt Runden ohne Positionierung, score=None Default, invalide Inputs landen sauber als None, Markdown-Tabelle bekommt Positionierung-Spalte wenn vorhanden, Tabelle ohne Positionierung-Spalte bei fehlenden Daten, fehlende Runden bekommen `-`, Arc + Positionierung nebeneinander, negativer Delta `(-N)`, ±0-Delta) + 8 in tests/test_pipeline_helpers.py (None-Report → None, leere Angles → None, kein_signal-only → None, Top-3 Average, Single-Angle, Rounding, 0-strength filtern, 100/100 Clamp). 506 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Positionierung-Top-N-Limit konfigurierbar:** Aktuell wird nur der stärkste Angle in `beginner_summary` gezeigt. AppConfig-Schalter `beginner_summary_positioning_limit: int = 1` (max 3) damit Autoren mit komplexer Story mehrere Angles sehen können. *(modules/config.py: neues `AppConfig.beginner_summary_positioning_limit: int = 1` Feld. `load_config` liest den YAML-Key und clampt auf [1, 3] über `max(1, min(3, int(...)))` — Werte <1 würden den stärksten Angle unterdrücken und so das ganze Positionierungs-Signal aus dem beginner_summary entfernen, Werte >3 würden Low-Strength-Rauschen einsammeln; beide Pathologien hart abgeschnitten. config.yaml: neuer Key mit Erklär-Kommentar (Default 1, 2-3 für Bücher mit mehreren Differenzierungs-Hooks). modules/pipeline.py: neue Konstante `TOP_POSITIONING_MAX_LIMIT = 3` als Single-Source-of-Truth für die Obergrenze (Config-Layer und Helper-Layer ziehen aus derselben Konstante). `_top_positioning_payload(positioning, *, limit=1)` filtert `kein_signal` und `strength<=0` _vor_ der Auswahl raus (statt nur am Top wie zuvor) und gibt jetzt zusätzlich `additional_angles: list[dict]` mit den Angles 2..N zurück — jeder Eintrag trägt `angle_key/claim/evidence/strength`, gebaut aus immutables, sodass Caller-Mutation die `DifferentiationAngle`-Quelle nicht erreicht. Backwards-Kompat: Top-Level-Keys (`angle_claim`/`angle_evidence`/`angle_strength`) und alle restlichen Felder bleiben unverändert; bei Default `limit=1` ist `additional_angles` einfach `[]`. `run_qa` reicht jetzt `self.config.beginner_summary_positioning_limit` durch. modules/industrial.py: `_render_top_positioning` rendert nach dem Stärkster-Angle-Bullet und seiner Beleg-Zeile pro Eintrag in `additional_angles` einen `🟢/🟡/🔴 **Weiterer Angle:** ... (Stärke: N/100)` Bullet plus optionale Beleg-Zeile. Whitespace-only `angle_claim` und non-dict Entries werden silently übersprungen — kein Stub-Bullet für malformed Payloads aus älteren Runden. 11 neue Unit-Tests: 7 in tests/test_pipeline_helpers.py (Default limit=1 → leere additional_angles, limit=2 zweiter Angle korrekt, limit=3 cap auf verfügbare Angles statt Padding, out-of-range limit=99 clampt auf TOP_POSITIONING_MAX_LIMIT, limit=0 coerce auf 1, Filter überspringt `kein_signal` und Strength=0 in jeder Position, Caller-Mutation der `additional_angles` greift nicht in die Quelle) + 3 in tests/test_industrial.py (zwei extras werden mit korrekten Belegen und Stärke-Werten gerendert, leere additional_angles → kein "Weiterer Angle"-Bullet, Whitespace-only claim wird übersprungen) + 4 in tests/test_config.py (Default 1, YAML-Lesen, Clamp >3 → 3, Clamp <1 → 1). 632 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Kollisions-Risiken-Top-1 in beginner_summary:** Wenn der Report ein hoch-priorisiertes Kollisions-Risiko enthält (z.B. fehlende Zahlen in KI-Nische), das Risiko als Warnzeile direkt unter dem Pitch im beginner_summary anzeigen. *(modules/pipeline.py: neuer pure helper `_top_collision_risk_payload(positioning)` liest die nach Schwere geordnete `collision_risks`-Liste aus `PositioningReport`, überspringt Whitespace-only Einträge und liefert ein immutables Dict mit `risk`, `niche_label`, `niche_confidence` und `total_risks` (volle Listengröße). Returns `None`, wenn keine Risiken vorliegen oder der erste echte Eintrag leer ist. modules/industrial.py: neuer Sub-Renderer `_render_top_collision_risk` zeichnet `## ⚠️ Kollisions-Risiko` direkt unter dem Positionierungs-Block mit 🔴-Badge, Risiko-Text, Nische und (bei total_risks>1) deutsch-pluralisiertem Verweis auf die weiteren Risiken im vollen Report. Sektion wird komplett weggelassen, wenn keine Daten oder das Risiko whitespace-only ist — keine Kosmetik ohne Signal. `render_beginner_summary` nimmt jetzt zusätzlich `top_collision_risk: dict | None`. `run_qa` reordered: `_top_collision_risk_payload(positioning)` wird neben `_top_positioning_payload` aus dem bereits berechneten Report-Objekt extrahiert — keine doppelte Berechnung, kein zusätzlicher DOCX-Read. 12 neue Unit-Tests: 5 in tests/test_pipeline_helpers.py (None-Report, leere Risk-Liste, Whitespace-Einträge überspringen, Top-1-Pick + Niche-Felder erhalten, total_risks reflektiert volle Liste, Immutability gegen Caller-Mutation) + 7 in tests/test_industrial.py (Sektion sichtbar mit 🔴-Badge + Risiko-Text + Nische + Filelink, fehlt bei None/empty, fehlt bei Whitespace-Risk, Plural vs Singular ("2 weitere Risiken" vs "1 weiteres Risiko"), keine "Außerdem"-Zeile bei total_risks=1, robust gegen partial payload ohne Niche/Total, Sektion erscheint nach `## Positionierung`-Block — direkt unter dem Pitch wie im Backlog gefordert). 352 Tests grün, kein bestehender Test gebrochen.)*

## Neu entdeckt — Kapitel-Reihungscheck-Folgearbeit

- [x] **Arc-Score im beginner_summary:** Wenn der Arc-Score <85 ist (oder Inversionen vorhanden), die Top-1-Reihenfolge-Korrektur direkt in beginner_summary.md aufnehmen ("Kapitel 3 → vor Kapitel 2 ziehen"), damit der Autor sofort weiß, wo der größte Hebel liegt. *(modules/pipeline.py: neuer pure helper `_top_arc_payload(arc_json)` extrahiert die Top-1-Strukturkorrektur aus `chapter_arc.json` (Fixes sind dort bereits nach Inversion → Missing-Phase sortiert), liefert `arc_score`, `status`, `top_fix`, `inversion_count`, `missing_count` — `None` wenn der Arc keine Fixes hat oder die Top-Fix-Zeile leer ist. modules/industrial.py: neuer Sub-Renderer `_render_top_arc()` zeichnet `## Reihenfolge` mit `score_badge`-Emoji, Score + Status, Top-Fix-Zeile und Inversion/Missing-Phase-Counter. `render_beginner_summary` nimmt jetzt zusätzlich `top_arc: dict | None`. `run_qa` reordered: `chapter_arc_review` läuft jetzt VOR `render_beginner_summary`, das Result wird an die nachfolgende Persistenz weitergereicht — keine doppelte Berechnung.)*
- [x] **Arc-Score in score_history.json mitschreiben:** Aktuell wird nur der Industrial-Score gespeichert. Eine zusätzliche `arc_score`-Spalte pro Runde würde zeigen, ob die Restrukturierung greift. *(modules/score_history.py: `ScoreHistoryEntry` um optionale `arc_score: int | None` und `arc_delta: int | None` Felder erweitert (default `None`, backwards-kompatibel für bestehende JSON-Files). Neuer pure helper `_coerce_optional_int(value)` der `int | None` zurückliefert (vs. `_coerce_int` der auf 0 fällt) — wichtig für die Unterscheidung "kein Arc verfügbar" vs. "Arc-Score 0". Neuer pure helper `_previous_arc_score(previous_entries)` walked die History rückwärts und liefert den letzten echten arc_score, sodass Runden ohne Arc-Analyse (z.B. wenn chapter_arc_review eine RuntimeError wirft) übersprungen werden — der Delta vergleicht immer gegen die letzte Runde mit Arc-Daten, nicht gegen `None`. `append_score_history(...)` nimmt jetzt zusätzlich `arc_score: int | None = None`; bei `None` bleibt `arc_score`/`arc_delta` ebenfalls `None`. `render_score_history_markdown` zeigt eine zusätzliche `Arc`-Spalte nur, wenn mindestens ein Eintrag einen arc_score hat (Runden ohne Arc nutzen `-`), Format `85/100 (+25)` / `60/100 (-15)` / `60/100 (±0)` / `60/100` (kein vorheriger Vergleich). modules/pipeline.py: `chapter_arc_review` läuft jetzt VOR `append_score_history`, der vorab berechnete `arc_score` wird durchgereicht — keine doppelte Berechnung, kein zusätzlicher DOCX-Read. 8 neue Unit-Tests: arc_score wird gespeichert mit/ohne previous, arc_delta überspringt Runden ohne Arc (vergleicht gegen letzte Arc-Runde), arc_score=None ist Default, invalide Inputs ("not-a-number") landen sauber als `None`, Markdown-Tabelle bekommt Arc-Spalte wenn vorhanden, Tabelle bleibt 6-spaltig wenn nie Arc, fehlende Runden bekommen `-` in der Arc-Spalte. 370 Tests grün, kein bestehender Test gebrochen.)*
- [ ] **LLM-Pass für Kapitel-Phasen-Klassifikation:** Aktuelle Klassifikation ist heuristisch (Marker-Regex + Positions-Bias). Optional eine LLM-Variante, die Kapitel-Synopsen liest und Phasen begründet — schaltbar via AppConfig.
- [x] **Manueller Phase-Override pro Kapitel:** Autor sollte in `metadata.md` mit `## Kapitel-Phasen` einen Override-Block hinterlegen können (Kapitel 1: PROBLEM, Kapitel 2: LÖSUNG …), den `chapter_arc` respektiert, falls die Heuristik die Phase verfehlt. *(modules/chapter_arc.py: `ChapterPhase` um `manual_override: bool = False` erweitert (default False — backwards-kompatibel für alle existierenden Konstruktor-Aufrufe). `to_json()` exportiert das Feld. Neuer pure helper `extract_phase_overrides(project) -> dict[int, str]` liest alle `.md`/`.txt`-Files aus `project.metadata_files + project.notes_files` und scrapt `## Kapitel-Phasen` / `## Chapter-Phases` Sektionen via `_PHASE_OVERRIDE_HEADER_RE`. Body-Zeilen-Regex `_PHASE_OVERRIDE_LINE_RE` matched `Kapitel 1: PHASE` / `1: PHASE` / `- 2: PHASE` / `3. 3: PHASE` (List-Präfixe gestrippt), die Phase wird via `_normalize_phase_alias()` auf den kanonischen Key gemappt — akzeptiert Umlaute (Lösung), ascii-folded (LOESUNG), Synonyme (methode→LÖSUNG, case→BEWEIS, wirkung/outcome→TRANSFORMATION, pain/schmerz→PROBLEM). Unbekannte Phasen werden silently skipped (Autor nicht für Freitext bestraft). Erste Definition pro Index gewinnt (Dedup). Tolerant gegen fehlende Files (OSError, UnicodeDecodeError). `build_arc_report(chapters, phase_overrides=None)` akzeptiert jetzt optional ein Override-Dict; pro Kapitel-Index wird nach der Heuristik geprüft, ob ein Override existiert. Override-Phase wird applied mit `confidence=100` und `manual_override=True`. Auch wenn die Override-Phase identisch zur Heuristik ist, wird `manual_override=True` gesetzt (Autor hat die Klassifikation explizit bestätigt). Unknown-Phase-Overrides werden silently ignoriert (Filter über `CANONICAL_PHASES`). modules/review.py: `chapter_arc_review(project)` ruft jetzt `extract_phase_overrides(project)` und reicht das Ergebnis durch — wirkt automatisch für alle Aufrufer (run_qa Pipeline). Markdown-Render: Confidence-Spalte trägt `*(manuell)*` Suffix für überschriebene Kapitel, damit der Autor in der Tabelle sieht, welche Phase aus dem Override kommt. 13 neue Unit-Tests in tests/test_chapter_arc.py decken: Default `manual_override=False` für backward-compat-Konstruktion, Override flippt Heuristik-Klassifikation, Override-confirming-heuristic markiert manual, unbekannte Phase-Keys werden ignoriert, Override verbessert Arc-Score wenn Reihenfolge korrigiert wird, Markdown zeigt `*(manuell)*`, Markdown ohne Override zeigt es nicht, Extractor liefert `{}` bei fehlender Sektion, Extractor parst kanonische Sektion mit allen 4 Phasen, akzeptiert Umlaute + Aliases (Lösung/methode/case/wirkung), strippt List-Präfixe (`-`/`*`/`N.`), skippt unbekannte Phasen, erste Definition gewinnt bei Duplikaten, tolerant gegen fehlende Files. 467 Tests grün, kein bestehender Test gebrochen.)*

## Neu entdeckt — Persona-Match-Folgearbeit

- [x] **Persona-Match-Highlight in beginner_summary:** Aggregat-Score + schwächste Persona aus dem persona_match-Report direkt in beginner_summary.md anzeigen, damit der Autor in Runde 1 sofort sieht, welche Persona ihre Beschreibung nicht trifft — ohne `buyer_personas.md` zu öffnen. *(modules/pipeline.py: neuer pure helper `_persona_match_payload(persona_match)` extrahiert einen kompakten Highlight-Dict mit `overall_score`, `status`, `description_present`, `total_personas`, `measurable_personas`, `weakest_label`, `weakest_score`, `weakest_missing`. Wählt die schwächste Persona deterministisch über `min(score, label)` (Tie-Break über Label-Alphabetik), filtert Personas mit `total_tokens=0` (nur Stop-Words, kein Signal). Returns `None` für: kein Report, keine Entries, keine messbaren Personas. modules/industrial.py: neuer Sub-Renderer `_render_persona_match_highlight()` zeichnet `## Persona-Match` mit `score_badge`-Emoji für Aggregat-Score, Status-Suffix in Klammern und einer optionalen Sub-Zeile für die schwächste Persona inkl. Top-3 fehlender Tokens. Bei `description_present=False` zeigt der Block den Hinweis "Keine Amazon-Beschreibung in den Metadaten — Match nicht messbar" ohne irreführenden 0-Score. `weakest_label`-Zeile wird übersprungen, wenn die Felder fehlen — partielle Payloads crashen nicht. `render_beginner_summary` nimmt jetzt zusätzlich `persona_match: dict | None = None`. Pipeline-Reorder: `build_persona_match_report` läuft jetzt VOR `render_beginner_summary`, das vorab berechnete Report-Objekt wird sowohl für die Highlight-Sektion als auch für die Persistenz in `buyer_personas.md`/`.json` wiederverwendet — keine doppelte Berechnung. Position der Sektion: zwischen `## Top-Persona` und `## Positionierung`, damit der Match direkt unter der Persona steht, der er gilt. 12 neue Unit-Tests: 7 in tests/test_pipeline_helpers.py (None-Input, leere Entries, nur Stop-Word-Personas, Weakest-Pick deterministisch, description_present-Flag, Immutability gegen Caller-Mutation, missing_tokens als Tuple ≤5) + 5 in tests/test_industrial.py (Sektion sichtbar mit Score + Status + Schwächster Persona + Filelink, fehlt bei None/empty, fehlt bei total_personas=0, Missing-Description-Hint statt 0-Score, partial payload ohne weakest_* zeigt keine "Schwächste Persona"-Zeile). 399 Tests grün, kein bestehender Test gebrochen.)*

## Neu entdeckt — Persona-Generator-Folgearbeit

- [x] **Persona-Top-Pick in beginner_summary:** Stärkste Persona (Persona #1) inkl. Problem + Kaufmotiv direkt in `beginner_summary.md` aufnehmen, damit der Autor sofort sieht, an wen er die ersten drei Beschreibungs-Zeilen schreiben muss. *(modules/pipeline.py: neuer pure helper `_top_persona_payload(persona_report)` selektiert Persona #1 (per Konvention die wahrscheinlichste Käuferin der Niche-Baseline), liefert immutables Dict mit `label`, `age_range`, `job`, `problem`, `buying_motive`, `anchor_quote`, `niche_label`, `niche_confidence`. modules/industrial.py: neuer Sub-Renderer `_render_top_persona()` zeichnet `## Top-Persona` mit Headline-Label, Alter, Job, Problem, Kaufmotiv und möglicher Suchanfrage. Sektion wird komplett weggelassen, wenn weder `problem` noch `buying_motive` Inhalt haben — keine Kosmetik ohne Signal. Empty-Label fällt auf `Persona 1` zurück. `render_beginner_summary` nimmt jetzt zusätzlich `top_persona: dict | None`. `run_qa` reordered: `build_persona_report` läuft jetzt VOR `render_beginner_summary` (Doppel-Build entfernt). 11 neue Unit-Tests: 6 in tests/test_pipeline_helpers.py (None-Report, leere Personas, Persona #1 wird gewählt, Niche-Felder erhalten, Immutability gegen Caller-Mutation, Single-Persona-Fallback) + 5 in tests/test_industrial.py (Sektion sichtbar mit allen Feldern + Filelink, fehlt bei None/empty, fehlt bei leerem Problem+Motiv, robust gegen partial payload ohne Alter/Quote, Empty-Label fällt auf `Persona 1`).)*
- [ ] **Persona-LLM-Pass:** Aktuelle Personas sind Template-basiert. Optional eine LLM-Variante, die aus Kapitel-Texten + Beschreibung echte O-Ton-Zitate der Persona ableitet ("So würde ich auf Amazon nach diesem Buch suchen") — schaltbar via AppConfig.
- [x] **Persona-vs-Beschreibung-Match-Score:** Berechne, wie viele Persona-Anker-Begriffe in den ersten 3 Zeilen der Amazon-Beschreibung auftauchen (0–100 Skala). Flag, wenn keine der Personas in den ersten Zeilen abgedeckt ist. *(modules/persona_match.py: `PersonaMatchEntry` + `PersonaMatchReport` (frozen dataclasses) + `build_persona_match_report(persona_report, amazon_description, *, lead_only=True)` + `render_persona_match_section(report)`. Pure-Python, deterministisch. Pro Persona werden Problem + Kaufmotiv + Anker-Quote zu einer Token-Liste ascii-gefaltet (Umlaute → ae/oe/ue/ss, lowercase), stop-word-gefiltert (deutsche + englische Filler), de-dupliziert und mindestens 4 Zeichen lang. Die Tokens werden gegen die ersten 3 Zeilen der Amazon-Beschreibung gemessen (`lead_only=True`, da das die Lines sind die der Kindle-Shopper vor "Mehr lesen" sieht) oder gegen die volle Beschreibung (`lead_only=False`). Score pro Persona = `round(100 * matched / total)`, Gesamt-Score = Mittelwert über alle Personas. Status-Schwellen 70/40 (READY/REVIEW/FIX) — eigene Schwellen statt der globalen 85/65, weil Token-Overlap heuristisch ist und ein Score von 70 für "die Beschreibung trifft 70% der Persona-Anker" bereits stark ist. Fehlende Beschreibung → `description_present=False`, Markdown zeigt klare "Keine Amazon-Beschreibung gefunden — Match nicht messbar"-Note statt irreführendem 0-as-failure. Personas mit total=0 (alle Tokens sind Stop-Words) bekommen Score 0 ohne division-by-zero. Missing-Tokens werden auf Top-5 gekürzt (für die Markdown-Tabelle), volle Listen bleiben in der JSON. Markdown rendert eine `## Match-Score gegen Amazon-Beschreibung`-Sektion mit Gesamt-Score, Status und einer `| Persona | Score | Treffer | Fehlt (Top 5) |`-Tabelle; bei 0 fehlenden Tokens steht ein em-dash. Pipeline-Wiring: `build_persona_match_report` wird nach `build_persona_report` aufgerufen, die Match-Section wird an `buyer_personas.md` angehängt, das JSON-Output trägt jetzt einen `match`-Subkey. Logger trackt `match_score` + `match_status` im `buyer_personas_completed`-Event. 17 neue Unit-Tests in tests/test_persona_match.py decken: None-Report, leere Personas, fehlende Beschreibung, Perfect-Overlap (READY), Zero-Overlap (FIX), Lead-Only-Modus ignoriert Text nach Zeile 3, Full-Mode findet Tokens unterhalb der Lead, Aggregation über mehrere Personas, Persona mit nur Stop-Words (kein crash), Missing-Tokens-Cap auf 5, Determinismus/Immutability, Render-Empty-State, Render-Missing-Description-State, Render-Tabelle, em-dash bei 0 Fehlern, Status-Schwellen 70/40, LEAD_LINE_COUNT==3. 387 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Persona-Kanal-Suggestion:** Pro Persona einen Vermarktungs-Kanal-Hinweis (z.B. CFO → LinkedIn, Solopreneur → LinkedIn + Twitter/X, Wechsler → Reddit) ergänzen, damit Launch-Pläne sich direkt darauf stützen können. *(modules/personas.py: `BuyerPersona` um `channels: tuple[str, ...]` Feld erweitert (default leeres Tuple — backwards-kompatibel für existierende Caller). Neuer pure helper `_suggest_channels(job, niche_key, flags)` mappt deterministisch: (1) Job-Keyword-Override (CFO/Controller → "LinkedIn (CFO/Controller-Gruppen)"; vertriebsleiter/head-of-sales → "LinkedIn (Sales-Navigator)"; solopreneur/freelance/agentur/trainer/berater → "LinkedIn + X (Solo-Business)"; geschäftsführer/inhaber/ceo → "LinkedIn (DACH-B2B-Decision-Maker)"; projektleiter/praktiker → "LinkedIn (Branchen-Gruppen)" — Override-Reihenfolge bewusst so gewählt, dass spezifische Solo/Sales-Rollen vor dem generischen "inhaber"-Match getroffen werden, damit "Agentur-Inhaber" nicht als CEO klassifiziert wird), (2) Flag-Channels (b2b → XING DACH-B2B; einsteiger → Reddit) werden VOR Niche-Defaults gepickt, damit ein gesetztes Flag den 3-Cap nicht verliert, (3) Niche-Defaults füllen den Rest aus einer 9-Nischen-Map (ki_und_ai → LinkedIn/X/Fachblog; finanzen_und_cfo → LinkedIn/XING/Finance-Newsletter; vertrieb_und_marketing → Sales-Navigator/XING/Sales-Podcasts; produktivitaet → LinkedIn/Newsletter/Podcasts; fuehrung → Leadership-Communities; selbststaendigkeit → LinkedIn/X/Solopreneur-Newsletter; immobilien → YouTube/Instagram/Facebook-Gruppen; mindset → Instagram/YouTube/Podcast-Interviews; allgemeines_sachbuch → Amazon-Anzeigen/LinkedIn/Themen-Newsletter), (4) Dedup über `in picks`-Check, (5) Cap auf `MAX_CHANNELS=3`. `build_persona_report` ruft den Helper pro Persona auf und übergibt Channels an `BuyerPersona`. `render_persona_report_markdown` zeichnet einen `- **Marketing-Kanäle:** ...`-Punkt unter jedem Persona-Block, weggelassen wenn die Persona kein Channel-Tuple trägt. `BuyerPersona.to_json` liefert `channels: list[str]`. 14 neue Unit-Tests in tests/test_personas.py decken: alle Personas haben Channels nach Build, Cap auf MAX_CHANNELS, CFO-Override, Sales-Override, Solo-Override (mit "Beraterin"-Keyword trifft Solo nicht CEO), Immobilien-Niche nutzt Visual-Channels, unbekannte Niche fällt auf allgemein zurück, B2B-Flag fügt XING hinzu (auch wenn niche-defaults schon den Cap füllen), Einsteiger-Flag-Verhalten, Render zeigt Marketing-Kanäle-Zeile, Render lässt Zeile weg wenn keine Channels, Default-Tuple-leer für Backwards-Compat, to_json-Serialisierung, Determinismus über zwei Builds. 427 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Persona-Override pro Buch:** Autor sollte in `metadata.md` mit `## Personas` einen Override-Block (3 Personas) hinterlegen können, der die niche-baseline ersetzt — z.B. für sehr spitze Nischen, die das Vokabular nicht trifft. *(modules/personas.py: neuer pure helper `extract_persona_overrides(project) -> list[dict[str, str]]` liest `.md`/`.txt`-Files aus `project.metadata_files + project.notes_files` und scrapt `## Personas` / `## Buyer Personas` Sektionen. Jeder Persona-Block startet mit `### Persona N: Label` (oder `### Label`), darauf folgen Field-Lines `- Alter: ...`, `- Job: ...`, `- Problem: ...`, `- Kaufmotiv: ...`, `- Suchanfrage: ...`. Feld-Aliases (case-insensitive, ascii-folded): `age/altersband`→age_range, `rolle/role/beruf`→job, `pain/schmerz`→problem, `motiv/motive`→buying_motive, `suche/anchor/query/search`→anchor_quote. Unknown-Field-Lines werden silently ignoriert (Autor kann eigene Notizen einstreuen). Erste Definition pro Feld gewinnt (Stabilität bei Duplikaten). `_parse_persona_block_label` strippt `Persona N:` Präfix damit der Subheader ein purer Label-String wird. Blöcke ohne actionable Field (label/job/problem) werden silently gedroppt. Tolerant gegen fehlende Files (OSError/UnicodeDecodeError). Cap auf `MAX_PERSONAS=3`. `build_persona_report` nutzt jetzt — wenn ≥1 Override vorhanden ist — die Author-Overrides statt der Niche-Baseline; jede Persona greift bei fehlenden Feldern auf den positionellen Niche-Baseline-Default zurück (Position 1 fällt auf erste Niche-Persona, Position 2 auf zweite, etc.) damit ein sparse Override (nur Label) immer noch ein renderable Result liefert. Channels werden weiterhin aus dem Override-`job` über `_suggest_channels` berechnet (kein Niche-Default-Channel-Verlust). `signal_flags` bekommt `persona_override` als Marker, wenn Overrides aktiv waren — gibt späteren Renderern und Tests einen klaren Hinweis. Längen-Caps (MAX_PROBLEM_CHARS/MAX_MOTIVE_CHARS/MAX_QUOTE_CHARS) bleiben erhalten, damit zu lange Override-Texte nicht das beginner_summary sprengen. 12 neue Unit-Tests in tests/test_personas.py decken: leere Sektion → leere Liste, voller Block mit allen Feldern, Aliases (age/role/pain/motive/query), Cap auf MAX_PERSONAS, unbekannte Felder ignoriert, leere Blöcke gedroppt, build verwendet Overrides, sparse Override (nur Label) erbt Baseline-Defaults, no-override behält Baseline, Override-Job triggert Solo-Business-Channel, fehlende Files tolerant, erste Definition wins bei Duplikaten. 491 Tests grün, kein bestehender Test gebrochen.)*

## Neu entdeckt — Score-Darstellung-Folgearbeit

- [x] **Badge auch in chapter_review/sample_scan/chapter_arc:** Aktuell setzen diese Reports zwar dieselben Emojis (🟢/🟡/🔴), nutzen aber lokale Konstanten. Auf `score_badge()` aus `industrial.py` umstellen, damit Änderungen an Skala oder Farben an einer Stelle passieren. *(Im Zuge des `modules/scoring.py`-Refactors mitgelöst: chapters.py, sample_scan.py und chapter_arc.py importieren jetzt `SCORE_READY`/`SCORE_REVIEW` und `SCORE_BADGE_READY`/`REVIEW`/`FIX` aus `modules.scoring`. Lokale `_STATUS_EMOJI`-Dicts werden aus den zentralen Konstanten gebaut, nicht mehr aus Inline-Strings — Skala oder Farben können an genau einer Stelle geändert werden.)*
- [x] **Gate-Übersicht in industrial_qa_report.md zusätzlich als kompakte Tabelle:** Aktuell pro Gate ein H3 mit Findings. Eine zusätzliche Tabellen-Sicht (Gate | Badge | Score | Status) am Anfang des Reports gibt eine schnelle Top-Level-Sicht für erfahrene Nutzer. *(modules/industrial.py: neuer Sub-Renderer `_render_gate_overview_table(report)` als Power-User-Pendant zu `_render_gate_overview` (Bullet-Liste fürs beginner_summary). Zeichnet `## Gate-Übersicht` mit `Skala: 🟢 ≥85 · 🟡 65–84 · 🔴 <65` Sub-Hinweis und einer markdown-Tabelle `| Gate | Badge | Score | Status |`. Wiederverwendet die `score_badge`/`GATE_DISPLAY_LABELS`-Mechanik: deutscher Display-Name, einheitliches Emoji, Score/100, Status. Respektiert blocking gates (explicit FIX trotz Score ≥65) — dasselbe Override wie in der Bullet-Liste damit beide Views stets synchron sind. Returns `[]` bei leerer/fehlender `gates`-Liste — keine Stub-Tabelle für unfertige Projekte. `render_industrial_qa_markdown` ruft den neuen Renderer JETZT VOR `## Gates`, sodass der Reviewer eine Top-Level-Sicht vor den Detail-H3-Sektionen mit Findings hat. 5 neue Unit-Tests in tests/test_industrial.py decken: Tabelle ist im industrial_qa_markdown vorhanden (Header, Skala, Pipe-Format), Tabelle kommt VOR den per-gate `## Gates`-Sektionen, eine Row pro Gate mit deutschem Label, jede Row trägt einen Score-Badge (READY/REVIEW/FIX-Emojis), leere `gates`-Liste produziert keine Tabelle. 544 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Score-Konstanten zentralisieren:** `SCORE_READY/REVIEW`-Konstanten leben aktuell parallel in industrial.py, chapters.py, chapter_arc.py, sample_scan.py. In ein gemeinsames `modules/scoring.py` ziehen, von dort importieren — verhindert Drift. *(modules/scoring.py: neue zentrale Quelle für `SCORE_READY=85`, `SCORE_REVIEW=65`, `SCORE_BADGE_READY/REVIEW/FIX` und die Status-Strings `STATUS_READY/REVIEW/FIX`. Zwei pure Helper: `score_badge(score, *, blocking=False, ready_threshold=85, review_threshold=65)` (gibt `(emoji, status)` zurück, `blocking=True` erzwingt FIX, optionale Schwellen erlauben alternative Skalen wie persona_match 70/40) und `status_for(score, *, blocking=False)` (Convenience-Wrapper). industrial.py importiert jetzt `SCORE_READY as SCORE_READY_THRESHOLD` / `SCORE_REVIEW as SCORE_REVIEW_THRESHOLD` / `score_badge` / `status_for as _status` aus `modules.scoring` — die lokale Definition von `score_badge`/`_status` wurde gelöscht und alle internen Aufrufe wurden auf `_status(score, blocking=blocking)` mit Keyword-Argument umgestellt. chapters.py, sample_scan.py und chapter_arc.py importieren `SCORE_READY`/`SCORE_REVIEW` aus `modules.scoring` und löschen ihre lokalen Definitionen; chapters.py + sample_scan.py bauen ihr lokales `_STATUS_EMOJI`-Dict jetzt aus den zentralen Badge-Konstanten statt aus Inline-Emojis. score_history.py importiert `score_badge` direkt aus `modules.scoring` statt re-exportiert aus `industrial.py` (industrial.py importiert score_history nicht — der Re-Export wäre ein toter Pfad). 13 neue Unit-Tests in tests/test_scoring.py decken: Schwellen-Konstanten, Badge-Emojis, READY-/REVIEW-/FIX-Grenzen (85/65), 100-Punkt-Score, blocking=True erzwingt FIX, custom-Thresholds (70/40 für alternative Skala), `status_for`-Wrapper, Zero-Score, Re-Export-Verträge für industrial/chapters/chapter_arc/sample_scan (Drift-Schutz: bricht, wenn ein Modul wieder eigene Konstanten setzt). 413 Tests grün, kein bestehender Test gebrochen.)*

## Neu entdeckt — Beginner-Summary-Folgearbeit

- [x] **Sample-Scan-Top-Risiko ebenfalls in beginner_summary:** Analog zu den schwächsten Kapiteln den schwächsten Sample-Abschnitt (höchstes Abbruch-Risiko) in die Summary aufnehmen — Daten existieren in `sample_scan.json`, nur die Pipeline-Reorder + Render-Block fehlen. *(Pipeline reordered, neuer Render-Block in industrial.py, `_weakest_sample_payload` in pipeline.py — Details siehe „Sample-Scan-Top-Risiko in beginner_summary" oben.)*

- [x] **Top-N Sample-Abschnitte in beginner_summary:** Aktuell nur Top-1-Risiko. Wenn z.B. drei Abschnitte FIX-Status haben, könnte ein optionaler Limit-Parameter (analog `weakest_chapters`-Limit) sichtbar machen, dass die Probleme zusammenhängen — sinnvoll erst, wenn das Manuskript >100k Wörter und damit >8 Sample-Abschnitte hat. *(modules/config.py: neues `AppConfig.beginner_summary_weakest_sample_limit: int = 1` Feld; `load_config` liest YAML-Key und clampt auf [1, 10]. config.yaml: neuer Key mit Erklär-Kommentar (Default 1, 2-3 für >100k-Wort-Sachbücher). modules/pipeline.py: neuer pure helper `_weakest_samples_payload(sample_json, *, limit=1) -> list[dict]` (immutable, sortiert nach `overall` aufsteigend, Tie-Break über `index` für stabile Reihenfolge, filtert READY-Sections raus, clampt limit auf [0, `WEAKEST_SAMPLE_MAX_LIMIT=10`], `limit=0` mutet die Sektion komplett). Bestehender `_weakest_sample_payload` jetzt dünner Backwards-Compat-Wrapper über den List-Helper — alle Bestandstests + Call-Sites bleiben grün. `run_qa` ruft den neuen List-Helper mit `self.config.beginner_summary_weakest_sample_limit` auf und reicht `weakest_samples` an `render_beginner_summary`. modules/industrial.py: neuer Sub-Renderer `_render_weakest_samples(weakest_samples)` mit dynamischem Heading — Singular `## Schwächster Sample-Abschnitt` bei 1 Section (backwards-kompatibel mit Bestandsruntime), Plural `## Schwächste Sample-Abschnitte` + Cluster-Issue-Intro bei ≥2. Neuer pure helper `_render_sample_section_line(section)` als Single Source of Truth für die Bullet+Fix-Zeile (gleiches Layout für single + multi). `_render_weakest_sample(single_dict)` ist jetzt dünner Wrapper über den List-Renderer. `render_beginner_summary` nimmt jetzt zusätzlich optional `weakest_samples: list[dict] | None = None`; wenn gesetzt, gewinnt der neue Parameter über das Legacy-`weakest_sample`-Dict — neue Pipeline-Call-Site nutzt die List-API, alte Test-Call-Sites mit einzelnem dict laufen unverändert weiter. 18 neue Unit-Tests: 4 in tests/test_config.py (Default 1, YAML-Lesen, Clamp >10 → 10, Clamp <1 → 1), 9 in tests/test_pipeline_helpers.py (Empty-Input → [], all-READY → [], ascending-Sort, Tie-Break-by-Index, Clamp auf verfügbare risky Sections, limit=0 mutet, Clamp auf MAX_LIMIT=10, Default-Limit=1, Field-Shape-Normalisierung, Backwards-Compat-Wrapper-Konsistenz), 5 in tests/test_industrial.py (Single-Section → Singular-Heading, Multi-Section → Plural-Heading + Cluster-Intro, leere Liste omits Section, neue List-API gewinnt über Legacy-Dict, Sortier-Reihenfolge im Output). 662 Tests grün, kein bestehender Test gebrochen.)
- [x] **Round-Delta-Highlight in beginner_summary:** Top-1-Highlight aus `round_delta.json` ("3 Fixes erledigt, Score +15") in die Summary aufnehmen, damit der Autor in Runde 2 sofort den Fortschritt sieht. *(modules/industrial.py: neuer Sub-Renderer `_render_round_delta_highlight` zeichnet `## Runden-Fortschritt` mit Aggregat-Zählern (resolved/persistent/new), Score-Delta inkl. signed-Badge (🟢 für +N, 🟡 für ±0/kein Vergleich, 🔴 für -N), Decision-Wechsel (🔁 GO_AFTER_FIXES → **GO**), Top-2 erledigte Fixes (✅) und Top-2 persistente Fixes (⚠️). Sektion wird komplett weggelassen, wenn es keine Vorrunde gibt (erste Pruefrunde) — keine Kosmetik, wenn nichts zu vergleichen ist. `render_beginner_summary` nimmt jetzt zusätzlich optional `round_delta_highlight: dict | None`. modules/pipeline.py: neuer pure helper `_round_delta_payload(delta, *, fix_limit=2)` konvertiert `RoundDelta` in einen kompakten Highlight-Dict, returnt `None` wenn `has_previous=False`, capped die Top-Fix-Listen unabhängig von den Aggregat-Zählern (counts spiegeln immer das volle Bild). `run_qa` reordered: `self.memory.compare_rounds()` wird jetzt VOR `render_beginner_summary` aufgerufen, das vorab berechnete `delta` wird an die nachfolgende round_delta-Persistenz weitergereicht — keine doppelte Berechnung. 13 neue Unit-Tests: 5 in tests/test_industrial.py (Sektion sichtbar mit allen Feldern, fehlt bei None/empty, robust gegen partial dicts, negativer Score nutzt 🔴-Badge, ±0 Score nutzt 🟡-Badge) + 8 in tests/test_pipeline_helpers.py (None-Delta, erste Runde → None, Counts korrekt, Score-Delta korrekt, Cap auf fix_limit, fix_limit=0 leert Listen, Score-Drop negativ, Decision unchanged Flag, Immutability der Top-Listen). 275 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Weakest-Chapter-Limit konfigurierbar:** Aktuell hart auf 3 — in AppConfig auslagern (`beginner_summary_weakest_limit: int = 3`) für Autoren mit sehr vielen Kapiteln. *(modules/config.py: neues `beginner_summary_weakest_limit: int = 3` Feld auf `AppConfig`, gelesen via `load_config` aus YAML mit Clamp `[1, 10]`; Default 3 matcht den vorherigen Hardcode 1:1 (kein Verhaltenswandel bei Bestands-Configs). Obergrenze 10 setzt eine harte Schwelle gegen "weakest" verliert seinen Sinn ab 10+ Kapiteln (wird zum Full-Report). modules/pipeline.py: `_weakest_chapter_payload(chapter_json, limit=self.config.beginner_summary_weakest_limit)` ersetzt den `limit=3`-Hardcode — single wiring point. config.yaml: neuer Key mit Kommentar dokumentiert (Empfehlung für Lesson-Style Sachbücher mit >20 Kapiteln: 5-7). 4 neue Unit-Tests in tests/test_config.py: Default 3, YAML-Wert wird übernommen, oberer Clamp 99→10, unterer Clamp 0→1. 644 Tests grün, kein bestehender Test gebrochen.)*

## Neu entdeckt — Kapitel-Balance-Folgearbeit

- [x] **Kapitel-Balance-Top-Pick in beginner_summary:** Den extremsten Outlier (oversized mit höchstem ratio ODER undersized mit niedrigstem ratio) als zusätzliche Zeile in beginner_summary.md aufnehmen, damit der Autor strukturelle Probleme ohne Öffnen von chapter_review.md sieht. *(modules/pipeline.py: neuer pure helper `_top_chapter_balance_payload(chapter_json)` vergleicht den ersten oversized (längstes Kapitel) und ersten undersized (kürzestes Kapitel) Outlier aus `chapter_review.json["balance"]` per absoluter Abweichung vom Median-Ratio 1.0 — wer weiter von 1.0 entfernt ist, gewinnt; Tie-Break bevorzugt `oversized` (Split-Entscheidung ist disruptiver). Returnt immutables Dict mit `kind`, `index`, `title`, `word_count`, `median`, `ratio`, `fix` — `None` wenn keine Balance-Daten, beide Listen leer oder Top-Fix whitespace-only. modules/industrial.py: neuer Sub-Renderer `_render_top_chapter_balance()` zeichnet `## Kapitel-Balance` mit Kind-Label (🔴 Split-Kandidat für oversized, 🟡 Merge-Kandidat für undersized), Kapitel-Identifikation (Index + Titel), Wörter-Zahl + Median-Vergleich, Ratio-Verhältnis und Fix-Zeile im Quote-Block. Sektion wird komplett weggelassen, wenn keine Daten oder kein Fix vorhanden ist — keine Kosmetik bei balancierten Büchern. `render_beginner_summary` nimmt jetzt zusätzlich `top_chapter_balance: dict | None`. Pipeline-Anschluss: `_top_chapter_balance_payload(chapter_json)` wird neben `_weakest_chapter_payload` aus dem bereits berechneten `chapter_json` extrahiert — kein zusätzlicher DOCX-Read, keine doppelte Balance-Analyse. 14 neue Unit-Tests: 9 in tests/test_pipeline_helpers.py (None-Report, fehlender balance-Key, leere Outlier-Listen, oversized-only Pick, undersized-only Pick, größere Deviation gewinnt bei beidseitigen Outliern, oversized-Bevorzugung bei Deviation-Tie, Whitespace-Fix → None, Immutability gegen Caller-Mutation) + 5 in tests/test_industrial.py (Sektion sichtbar mit allen Feldern + Split-Label + Filelink, undersized nutzt 🟡-Badge + Merge-Label, fehlt bei None/empty, fehlt bei whitespace fix, partial payload fällt auf `Kapitel N — Kapitel N` zurück). 339 Tests grün, kein bestehender Test gebrochen.)
- [x] **Balance-Schwellen konfigurierbar:** `OVERSIZED_FACTOR`/`UNDERSIZED_FACTOR`/`BALANCE_MIN_CHAPTERS` in AppConfig auslagern, damit Autoren mit Sachbüchern aus kurzen Lektionen (z.B. 30 Mini-Kapitel) andere Schwellen setzen können. *(modules/chapters.py: neue frozen-dataclass `BalanceThresholds` mit drei Feldern (`oversized_factor`, `undersized_factor`, `min_chapters`) — Defaults matchen 1:1 die alten Modul-Konstanten, sodass alle Bestandstests unverändert grün laufen. `DEFAULT_BALANCE_THRESHOLDS`-Singleton + neuer pure helper `balance_thresholds_from_app(app_config)` als single wiring point. `analyze_chapter_balance` nimmt jetzt optional `thresholds: BalanceThresholds | None`; bestehende Kwargs `oversized_factor/undersized_factor/min_chapters` bleiben als explizite Overrides erhalten und trumpfen den Threshold-Object (Backwards-Compat für In-Tree-Caller). `build_chapter_report` nimmt jetzt `balance_thresholds=None` und reicht es durch — Default-Aufruf bleibt unverändert. modules/review.py: `chapter_review(project, *, balance_thresholds=None)` propagiert die Schwellen — der Pipeline-Caller ist der einzige der echte Werte einsetzt. modules/config.py: AppConfig um drei Felder erweitert (`balance_oversized_factor: float = 3.0`, `balance_undersized_factor: float = 0.3`, `balance_min_chapters: int = 3`); `load_config()` liest sie aus YAML mit aggressivem Clamping (`oversized_factor` auf `[1.1, 20.0]`, `undersized_factor` auf `[0.01, 0.9]` — beide Bounds verhindern Pathologie wie `oversized < 1.0` was jedes Kapitel als Outlier flaggen würde, oder `undersized > 1.0` was die Logik invertieren würde; `min_chapters` Floor 2 verhindert dass ein single-chapter Buch noch analysiert wird). config.yaml: drei neue Keys mit Kommentar dokumentiert (Empfehlung für Lesson-Style Sachbücher mit ~30 Mini-Kapiteln: `oversized_factor: 2.0`, `undersized_factor: 0.5`). modules/pipeline.py: neuer Import `balance_thresholds_from_app`; `run_qa` ruft jetzt `chapter_review(project, balance_thresholds=balance_thresholds_from_app(self.config))` — wirkt automatisch ohne weitere Caller-Änderungen. 11 neue Unit-Tests in tests/test_chapters.py: Default-Thresholds matchen Legacy-Konstanten (Drift-Schutz), `BalanceThresholds` ist frozen, tighter `oversized_factor=2.0` flaggt 2.5×-median Kapitel die Default 3.0× durchgehen lässt, lockerer `undersized_factor=0.5` markiert zusätzlich 0.4×-median Kapitel als MERGE-Kandidat, `min_chapters=2` aktiviert Balance-Analyse für 2-Kapitel-Bücher die Default 3 sonst überspringen würde, Legacy-Kwarg `oversized_factor=2.0` überschreibt `thresholds.oversized_factor=3.0` (Backwards-Compat), `build_chapter_report` propagiert die Thresholds (gleiches Outlier-Bild wie der direkte `analyze_chapter_balance`-Call), `balance_thresholds_from_app` liest die drei AppConfig-Felder verbatim, `load_config` liest die drei YAML-Keys, `load_config` defaultet auf 3.0/0.3/3 wenn der Block fehlt, `load_config` clampt invalide Werte (`oversized=0.5`→1.1, `undersized=1.5`→0.9, `min_chapters=1`→2). 596 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Balance-Score im score_history.json:** Anteil der Kapitel, die innerhalb der Balance-Grenzen liegen, als zusätzliche `balance_score`-Spalte pro Runde — würde zeigen, ob die Restrukturierung die strukturelle Verteilung verbessert. *(modules/pipeline.py: neuer pure helper `_balance_score(chapter_json)` berechnet 0–100-Score als `round(100 * (total - outliers) / total)`, wobei `outliers = len(oversized) + len(undersized)` aus `chapter_json["balance"]`. Returns `None`, wenn kein chapter_json, leere Kapitel-Liste, fehlender/non-dict `balance`-Key (z.B. unter `BALANCE_MIN_CHAPTERS`) — Score-History zeigt dann `-` statt einer irreführenden 0. Defensive Clamps: outliers>total wird zu 0 (statt negative Score), Ergebnis wird auf [0,100] geclamped. modules/score_history.py: `ScoreHistoryEntry` um optionale `balance_score: int | None` + `balance_delta: int | None` erweitert (default `None`, backwards-kompatibel). `append_score_history(...)` nimmt jetzt zusätzlich `balance_score: int | None = None`; nutzt den existierenden `_previous_optional_field`-Helper für Delta gegen die letzte Runde mit echtem balance_score (überspringt Runden ohne Balance-Analyse). `render_score_history_markdown` ergänzt eine konditionale `Balance`-Spalte (header_cells + row builder) zwischen Positionierung und Decision — Spalte erscheint nur, wenn mindestens ein Eintrag einen balance_score trägt. Format folgt der gemeinsamen `_format_optional_score_cell`-Logik: `85/100`, `85/100 (+25)`, `90/100 (-10)`, `60/100 (±0)` oder `-` für leere Runden. modules/pipeline.py: `_balance_score(chapter_json)` wird neben `_positioning_score(positioning)` aus dem bereits berechneten `chapter_json` extrahiert und an `append_score_history` weitergereicht — keine doppelte Berechnung, kein extra DOCX-Read. 17 neue Unit-Tests: 9 in tests/test_pipeline_helpers.py (None-Input, leere Kapitel-Liste, fehlende Balance-Daten → None, perfekt 100 ohne Outlier, Score-Drop mit Oversized, Sum aus oversized+undersized, Clamp bei mehr Outliern als Kapiteln, Rounding-Verhalten, non-dict balance ignoriert) + 8 in tests/test_score_history.py (Score wird gespeichert, Delta gegen vorigen Eintrag, Delta überspringt Runden ohne Balance, Default `None`, invalid Input → None, Markdown-Tabelle mit Balance-Spalte, Tabelle ohne Balance-Spalte wenn keine Daten, fehlende Runden bekommen `-`, Arc + Positionierung + Balance nebeneinander). 539 Tests grün, kein bestehender Test gebrochen.)*

## Neu entdeckt — Lesbarkeits-Folgearbeit

- [x] **Lesbarkeitsanalyse mit Amstad-FRE (German Flesch-Reading-Ease):** Eigener Lesbarkeits-Gate auf Basis der Amstad-Adaption (`FRE = 180 - ASL - 58.5 × ASW`) ergaenzt die bisherige heuristische 1-10-Lesbarkeits-Note in sample_scan um eine standardisierte, vergleichbare 0-100-Skala mit Niveau-Baendern (A1-C2). Direkt actionable: "Kapitel 3 liegt bei FRE 38 - Sachbuch-Zielgruppe will FRE 50-80." *(modules/readability.py: neues pure-Python-Modul. `compute_amstad_fre(text)` liefert `(fre, words, sentences, syllables, asl, asw)` ueber deterministische Tokenizer: `_WORD_RE` (Buchstaben+Bindestrich, ignoriert reine Zahlen-Tokens), `count_sentences` (kollabiert `.{2,}` auf einen Terminator damit Ellipsen die Saetze nicht inflationieren, Floor 1 bei Text ohne Punkt), `count_syllables_de` (Vokalgruppen-Heuristik a/e/i/o/u/ae/oe/ue/y - Diphthonge wie ei/ie/au/eu zaehlen als eine Vokalgruppe, Floor 1 fuer vokalfreie Tokens). `classify_fre(fre)` mappt auf 7 Amstad-Baender (sehr_leicht 90+, leicht 80+, mittel_leicht 70+, mittel 60+, mittel_schwer 50+, schwer 30+, sehr_schwer <30) mit deutschen Niveau-Labels. `ReadabilityMetric` (frozen) und `ReadabilityReport` (frozen) als immutable Datenstrukturen mit `to_json()`. `build_readability_report(chapters, *, target_min=50, target_max=80)` baut pro Kapitel + Aggregate (ueber konkateniertes Body) eine Metrik und waehlt das `weakest_index` als das Kapitel mit der groessten Abweichung vom Zielband - Kapitel im Band haben keinen weakest. Konkrete Fix-Zeilen werden generiert: zu schwer -> "lange Saetze kuerzen, Fachjargon ersetzen, aktive Verben", zu leicht -> "Tiefe und Fachpraezision pruefen", zu kurz fuer Signal (<60 Woerter) -> "Inhalt ausbauen oder mit Nachbarkapitel verschmelzen". `render_readability_markdown(title, report)` rendert `# Lesbarkeit (Amstad-FRE)` mit Skala-Hinweis, Gesamt-Block (Woerter/Saetze/Silben/ASL/ASW/FRE), Pro-Kapitel-Tabelle mit Status-Badges (READY/REVIEW/FIX abhaengig vom Abstand zum Zielband), Konkrete-Fixes-Liste und Schwaechstes-Kapitel-Sektion. modules/review.py: neuer `readability_review(project, *, target_min=50, target_max=80) -> tuple[str, dict]` wrappt `readability_analysis_from_project` und liefert (md, json) - pure-Python, kein API-Key noetig. modules/pipeline.py: `run_qa` ruft `readability_review` nach `chapter_review` auf, schreibt `readability.md` + `readability.json` und loggt `readability_completed` mit fre_score und weakest_index. Beide Artefakte sind in QUICK_ROUND_ARTIFACTS (modules/rounds.py) und der Pipeline-Mirror-Liste eingetragen. 42 neue Unit-Tests in tests/test_readability.py decken: Silben-Counting (1-, 2-, 3-Silben-Wortbeispiele, Umlaute, Konsonant-Only-Floor, Case-Insensitivity), Wort-Counting (empty, simple, Bindestrich-als-eins, Zahlen-ignore, Umlaute), Satz-Counting (empty, single, multi, no-terminator-Floor, Ellipsen-Kollabierung), Amstad-FRE (empty-Sentinel, leichter Text scored hoeher als komplexer Text, Approximation), Klassifikation (alle 4 getesteten Baender, negative-Clamp, Label-Uniqueness), Report-Building (empty, single-chap, weakest-Pick, all-in-target-no-weakest, fixes-nur-outside-target, kurz-flagged, custom-Target-Band, frozen, JSON-Roundtrip, is_in_target), Markdown-Render (Header/Band, Tabelle, empty-Handling, weakest-Section, Fixes-Section). 704 Tests grueen insgesamt, kein bestehender Test gebrochen.)*
- [x] **Lesbarkeits-Score in score_history.json:** Aggregat-FRE pro Runde als `readability_score`-Spalte aufnehmen, damit der Autor sieht ob seine Vereinfachungs-Iterationen die Zielgruppe besser treffen. *(modules/score_history.py: `ScoreHistoryEntry` um `readability_score: int | None` und `readability_delta: int | None` erweitert (frozen-dataclass, defaults None — backwards-kompatibel mit allen Bestandseintraegen ohne FRE). `append_score_history()` nimmt jetzt zusaetzlich `readability_score: int | None`, berechnet das Delta gegen den juengsten vorigen Eintrag der einen Wert traegt (skipt Runden ohne Messung — gleiche Semantik wie `arc_score`/`positioning_score`/`balance_score`, ueber den vorhandenen `_previous_optional_field`-Helper). `to_json()` serialisiert beide neuen Felder. `render_score_history_markdown()` zieht eine neue `Lesbarkeit`-Spalte ein, wenn mindestens ein Eintrag einen Wert traegt — fehlende Eintraege erscheinen als `-`, Deltas als `+N`/`-N`/`±0` wie alle anderen optionalen Metriken. modules/pipeline.py: neuer pure helper `_readability_score(readability_json) -> int | None` rundet `overall.fre_score` zu int und clamped defensiv auf `[0, 100]`. Returns `None` bei: keinem JSON, fehlendem `overall`, `word_count<60` (Mirror von `READABILITY_HIGHLIGHT_MIN_WORDS`), fehlendem/nicht-numerischem FRE oder nicht-int word_count — score_history zeigt dann einen Strich statt einer luegenden Null. `run_qa` reorderd: `readability_review` laeuft jetzt VOR `append_score_history`, der Score wird durchgereicht; der doppelt vorhandene Block weiter unten in der Pipeline wurde entfernt (vermeidet zweite DOCX-Lesung). 19 neue Unit-Tests: 9 in tests/test_score_history.py (records-when-provided, delta-against-previous, skips-rounds-without-readability, optional-defaults-to-none, invalid-input-graceful, markdown-column-when-any-entry-has-it, markdown-omits-column-when-none, markdown-uses-dash-for-missing, negative-delta-with-minus, zero-delta-plus-minus, all-four-metric-columns-side-by-side) + 10 in tests/test_pipeline_helpers.py (None/empty input, rounded int, half-up rounding, word_count below min, fre missing, fre not numeric, overall missing, negative-fre clamped to 0, above-100 clamped to 100, non-int word_count). 744 Tests gruen, kein bestehender Test gebrochen.)*
- [x] **Lesbarkeits-Highlight in beginner_summary:** Aggregat-FRE + Niveau-Label + schwaechstes Kapitel direkt in beginner_summary.md anzeigen, damit der Autor in Runde 1 sofort sieht ob die Sprache zur Zielgruppe passt. *(modules/pipeline.py: neuer pure helper `_readability_highlight_payload(readability_json)` extrahiert ein immutables Dict mit `overall_fre` (gerundet), `level_label`, `target_min`/`target_max`, `in_target` (bool), `overall_fix`, `weakest_label`/`weakest_fre`/`weakest_fix`. Returns `None` wenn das Manuskript zu kurz ist (`READABILITY_HIGHLIGHT_MIN_WORDS=60`, gespiegelt aus `modules.readability.MIN_BODY_WORDS_FOR_SIGNAL` ohne Kopplung) oder wenn `fre_score` fehlt/nicht-numerisch ist — keine Kosmetik-Section "FRE 0.0" wenn nichts gemessen werden konnte. Pipeline reorderd: `readability_review(project)` wird jetzt VOR `render_beginner_summary` ausgeführt, das vorab berechnete `readability_md/_json` an die nachfolgende Persistenz weitergereicht — keine doppelte DOCX-Lesung. modules/industrial.py: neuer Sub-Renderer `_render_readability_highlight(payload)` zeichnet `## Lesbarkeit` mit Badge (🟢 in_target, 🟡 ≤20 Punkte ausserhalb, 🔴 >20 Punkte ausserhalb — gespiegelt aus `_badge_for_metric`), Gesamt-FRE-Zeile mit Level-Label, target-Band-Hinweis (3 Varianten: in_target / zu schwer / sehr einfach) und entweder einer "Schwaechstes Kapitel (Lesbarkeit)"-Sektion mit Fix-Quote oder einem "Top-Fix"-Block bei Aggregat-Fix ohne Weakest. Sektion wird komplett weggelassen wenn `payload` None/empty/`fre` non-numeric. `render_beginner_summary` nimmt jetzt zusätzlich `readability_highlight: dict | None`, Sektion wird nach `_render_amazon_html_preview` ausgegeben (gleicher Bereich wie die anderen Listing-relevanten Hinweise, vor "Was bedeutet das praktisch?"). 17 neue Unit-Tests: 10 in tests/test_pipeline_helpers.py (None-Input, empty-dict, word_count unter Min → None, FRE missing → None, FRE not-numeric → None, in-target reports correctly, below-band, above-band, weakest chapter carried, partial chapter dict, Immutability gegen Caller-Mutation, weakest_index pointing to missing chapter), 7 in tests/test_industrial.py (Section sichtbar in-target mit FRE+Label+Band+Filelink, fehlt bei None/empty, 🔴 bei FRE<target_min-20, 🟡 bei FRE>target_max ≤20, 🟡 bei near-miss FRE<target_min ≤20, Weakest-Block mit FRE+Label+Fix, robust gegen partial Payload). 723 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Lesbarkeits-Zielband konfigurierbar:** AppConfig-Schalter `readability_target_min: int = 50` / `readability_target_max: int = 80` damit Autoren mit Fachbuechern (Ziel z.B. 30-50) oder Lifestyle-Buechern (60-90) die Zielzone anpassen koennen. *(modules/config.py: zwei neue `AppConfig`-Felder `readability_target_min: int = 50` und `readability_target_max: int = 80`. Neuer pure helper `_readability_target_band(data)` liest beide YAML-Werte, coercet zu int (Fallback auf Default bei Nicht-Numerisch wie `fifty`), clamped jede Seite in den Amstad-Sane-Range `[10, 100]` und faellt auf das kanonische 50/80-Default zurueck wenn die Band degeneriert ist (min >= max — Tippfehler oder Clamping-Kollaps). `load_config` ruft den Helper einmal vor der `AppConfig`-Konstruktion auf. config.yaml: Dokumentationsblock mit 3 Zielgruppen-Beispielen (Fachbuch 30-55, Sachbuch 50-80, Lifestyle 60-90) und Hinweis auf Clamping + Degeneriert-Fallback. modules/pipeline.py: `run_qa` reicht `target_min=self.config.readability_target_min` und `target_max=self.config.readability_target_max` an `readability_review()` durch (statt die alten DEFAULT_TARGET_MIN/MAX-Konstanten zu verwenden). 9 neue Unit-Tests in tests/test_config.py (Default 50/80, custom Fachbuch-Band 30/55, Lifestyle 65/95, Clamp below floor zu 10, Clamp above ceiling zu 100, Fallback bei min > max, Fallback bei min == max, Fallback bei nicht-numerischen YAML-Werten) + 2 neue Tests in tests/test_readability.py::TestReadabilityReviewBandPropagation (default 50/80 in Payload, custom 30/55 propagiert in Payload — pinnt die public-API-Vertragsoberflaeche). 754 Tests gruen, kein bestehender Test gebrochen.)*


## Neu entdeckt — Amazon-Description-HTML-LLM-Pass-Folgearbeit

- [x] **LLM-Bullets-Highlight in beginner_summary:** Wenn LLM-Bullets generiert wurden, eine kleine Marker-Zeile ("Bullets aus LLM-Pass") in beginner_summary.md aufnehmen, damit der Autor weiss ob er die Template- oder die Manuskript-spezifischen Bullets vor sich hat. *(modules/pipeline.py: `_amazon_html_preview_payload(amazon_html)` extrahiert jetzt zusaetzlich `bullets_source` aus dem `AmazonDescriptionHtml`-Snippet, validiert gegen die kanonische `BULLETS_SOURCES`-Allowlist und faellt bei unbekannten/leeren Werten defensiv auf `BULLETS_SOURCE_TEMPLATE` zurueck — backwards-kompatibel mit Legacy-Snippets ohne Source-Attribut. modules/industrial.py: neue Modul-Konstante `BULLETS_SOURCE_LABELS` mappt die drei kanonischen Source-Keys auf deutsche Markerlinien (`Bullets aus LLM-Pass (manuskript-spezifisch generiert)`, `Bullets aus bestehender Beschreibung übernommen`, `Bullets aus Template-Bibliothek (kein LLM-Pass aktiv)`). `_render_amazon_html_preview` zieht das Label aus dem Payload, rendert es als `_Quelle: …_`-Zeile direkt unter dem erklärenden Subtitle und ueber dem Preview-Quote — bei fehlendem ODER unbekanntem Source-Wert wird die Zeile komplett weggelassen, damit Legacy-Payloads exakt wie vorher rendern. 5 neue Unit-Tests in tests/test_pipeline_helpers.py (Default `template` bei fehlendem Attribut, LLM-Source durchgereicht, Existing-Source durchgereicht, unbekannter Source faellt auf `template`, Whitespace-Stripping) + 5 neue Unit-Tests in tests/test_industrial.py (LLM-Marker mit "LLM-Pass"+"manuskript-spezifisch", Existing-Marker mit "bestehender Beschreibung" und ohne LLM-Hinweis, Template-Marker mit "Template"+"kein LLM-Pass", unbekannter Source-Wert rendert Preview ohne Marker-Zeile, fehlender Source-Key rendert Preview ohne Marker-Zeile fuer Backwards-Compat). 846 Tests gruen, kein bestehender Test gebrochen.)*
- [x] **LLM-Bullets-Quelle in amazon_description.json mitschreiben:** Ein zusaetzliches Feld `bullets_source: "llm"|"existing"|"template"` in `amazon_description.json` damit downstream Tools (Release-Packager, CI) den Unterschied erkennen koennen ohne die HTML zu parsen. *(modules/amazon_html.py: drei neue Modul-Konstanten `BULLETS_SOURCE_LLM="llm"`, `BULLETS_SOURCE_EXISTING="existing"`, `BULLETS_SOURCE_TEMPLATE="template"` plus `BULLETS_SOURCES`-Tuple als Drift-Schutz (kanonische Liste, drei distinkte Strings). `AmazonDescriptionHtml` (frozen dataclass) um `bullets_source: str = BULLETS_SOURCE_TEMPLATE` erweitert — Default matched den dominanten Fallback-Pfad, damit alle Bestandskonstruktoren ohne Sources-Argument backwards-kompatibel `"template"` liefern. `to_json()` exportiert das Feld als String-Wert. `_select_bullets(...)` gibt jetzt `tuple[list[str], str]` statt nur die Liste zurück — drei distinkte Source-Branches: (a) LLM-Bullets ≥MIN_BULLETS UND Quality-Check `passed` → `BULLETS_SOURCE_LLM`; (b) `_extract_existing_bullets` ≥MIN_BULLETS → `BULLETS_SOURCE_EXISTING`; (c) Template-Fallback (auch wenn Existing-Bullets als Header mit reingespliced wurden — die Mehrheit kommt vom Template) → `BULLETS_SOURCE_TEMPLATE`. `build_amazon_description_html(...)` packt den Source-Label in das frozen-Result. Pipeline schreibt das Feld automatisch via existierenden `to_json()`-Call in `amazon_description.json` — keine pipeline.py-Änderung nötig. 8 neue Unit-Tests in tests/test_amazon_html.py: Konstanten-Drift-Schutz (drei distinkte Strings, alle in `BULLETS_SOURCES`), Default ohne LLM/Existing-Bullets → `template`, Existing-Marker-Beschreibung mit 5 Bullets → `existing` (über `- `/`*`/Nummerierung erkannt), LLM-Bullets mit gutem Material → `llm`, LLM-Bullets <MIN_BULLETS → Fallback (kein `llm`-Label), LLM-Bullets mit Hype-Tokens fallen Quality-Check und verlieren `llm`-Label, `to_json()` serialisiert das Feld und das Resultat liegt in `BULLETS_SOURCES`, AmazonDescriptionHtml-Konstruktor ohne expliziten Source defaultet auf `template` (Backwards-Compat-Vertrag für Test-Helper und externe Aufrufer). 836 Tests grün, kein bestehender Test gebrochen.)*
- [x] **Sample-Section + erste Kapitel-Absaetze in LLM-Bullet-Prompt:** Aktuell nur Titel/Untertitel/Description/Kapitel-Titel. Der erste Absatz jedes Kapitels (max 400 Zeichen) wuerde die LLM-Bullets noch enger an das echte Manuskript binden statt nur an die TOC — pure-Python-Extraktor aus chapters.py wiederverwendbar. *(modules/chapters.py: neue Modul-Konstante `CHAPTER_INTRO_MAX_CHARS=400` als Drift-Schutz fuer das Prompt-Size-Budget, plus pure-Python-Helfer `extract_chapter_intros(chapters, *, max_chars=400)` der `(title, first-paragraph)`-Tuples zurueckgibt. Interne Helfer `_first_paragraph(body)` (zieht die erste nicht-leere Zeile, ueberspringt fuehrenden Whitespace) und `_clip_intro(text, max_chars)` (haengt `…` bei Truncation an, returns `""` wenn `max_chars<=0`) — beide deterministisch und ohne Mutation der `Chapter`-Inputs. modules/amazon_html.py: neue Konstante `LLM_BULLETS_MAX_CHAPTER_INTROS=12` als Cap auf das Prompt-Blockwachstum bei Buechern mit vielen Kapiteln; `build_llm_bullets_user_prompt(...)` akzeptiert jetzt optional `chapter_intros: Sequence[tuple[str, str]] | None = None` und rendert bei nicht-leerem Input einen separaten `Kapitel-Eroeffnungen (jeweils erster Absatz, gekuerzt):`-Block via `_render_chapter_intros_block(...)` der whitespace-only Intros silent skippt (verhindert `- Kapitel 3: `-Dangler) und an `LLM_BULLETS_MAX_CHAPTER_INTROS` cappt. Bei nichts-zu-zeigen wird der gesamte Block weggelassen — keine leeren Sektionen im Prompt. `extract_amazon_bullets_via_llm(...)` reicht den neuen Parameter weiter (kwarg-only, Backwards-compat-Vertrag fuer existierende Caller). modules/pipeline.py: neuer `_collect_chapter_intros(project)`-Helper laedt das Manuskript via `extract_docx_chapters` und ruft `extract_chapter_intros` — Exception-safe (logging.warning bei Lesefehler, leere Liste statt Crash, kein Manuscript => `[]`). `_maybe_extract_amazon_llm_bullets` ruft jetzt diesen Helper, reicht das Result als `chapter_intros=chapter_intros or None` weiter und ergaenzt im Erfolgs-Log-Event ein `intro_count`-Feld fuer die Run-Traces. 8 neue Unit-Tests in tests/test_chapters.py decken den Intro-Helper (Default `max_chars=400`, erste-Zeile-Extraktion, Clipping mit Ellipsis, Short-Intro-no-Clip, leere Body-Variants, Whitespace-Skip, No-Mutation, Empty-Input, `max_chars=0`-Edge-Case) plus 7 neue Tests in tests/test_amazon_html.py (`LLM_BULLETS_MAX_CHAPTER_INTROS>=1`-Drift-Schutz, ohne Intros kein Block, mit Intros rendert "Kapitel-Eroeffnungen", Whitespace-only Intros werden skippt ohne Dangler, alle-leer => kein Block, Cap auf `LLM_BULLETS_MAX_CHAPTER_INTROS`, `extract_amazon_bullets_via_llm` reicht Intros bis ins User-Prompt durch, kwarg-only Backwards-Compat). 863 Tests gruen, kein bestehender Test gebrochen.)*
- [x] **Bullet-Quality-Check fuer LLM-Output:** Sanity-Check der LLM-Bullets vor Ueberbleibsel: Anti-Hype-Filter (kein "!", keine "ultimativ"/"unglaublich"/"perfekt"), keine doppelten Anfangsworte, mindestens eine Zahl in mindestens einer Bullet. Bei Verstoss faellt das Modul wieder auf Template zurueck. *(modules/amazon_html.py: neuer `BulletQualityResult` (frozen dataclass) mit `accepted`/`rejected`/`violations`/`passed`-Feldern als immutable Single-Source-Wahrheit der Quality-Entscheidung. Neue Modul-Konstanten als Drift-Schutz: `LLM_BULLETS_MIN_NUMBER_HITS=1` (mindestens eine Bullet mit Ziffer), `LLM_BULLETS_HYPE_TOKENS` (26 deutsche+englische Marketing-Hype-Tokens — ultimativ/unglaublich/perfekt/garantiert/revolutionär/weltbeste/bestseller/sensationell/fantastisch/magisch/geheim/phänomenal/game-changer etc. mit Umlauten + ascii-Varianten). Vorkompilierte Regex: `_LLM_BULLETS_HYPE_RE` (Wort-Boundary + längste-zuerst-sortiert damit `must have` vor `must-have` greift), `_LLM_BULLETS_DIGIT_RE`, `_LLM_BULLETS_FIRST_WORD_RE` (Unicode-aware, ignoriert führende Satzzeichen sodass `Drei,` und `Drei` dieselbe Start-Word-Key bekommen). Pure-Python-Helper `validate_llm_bullets(bullets)` läuft zwei Pässe: (1) per-bullet rejection — non-string ("non_string"), leerer String (silently skipped), `!` enthalten ("contains_exclamation"), Hype-Token enthalten ("contains_hype:<token>"); (2) Aggregat-Validation auf den überlebenden Bullets — `missing_number` wenn keine accepted Bullet mindestens eine Ziffer trägt, `duplicate_start_word` wenn 2+ Bullets mit derselben First-Word-Lemma starten, `all_rejected` wenn der Per-Bullet-Filter alles weggekehrt hat. `passed=True` nur wenn `violations=()` UND `len(accepted) >= MIN_BULLETS=4` — sonst fällt der Caller auf den Template-Pfad zurück. `_select_bullets` wurde minimal-invasive um den Quality-Check erweitert: nach `_normalize_bullet_candidates` (existing length/dedupe) läuft `validate_llm_bullets(llm_clean)`; nur bei `quality.passed` werden die akzeptierten Bullets verwendet, sonst gehts in den existierenden existing/template-Fallback-Pfad. 15 neue Unit-Tests in tests/test_amazon_html.py decken: gute Bullets passen alle, `!`-rejection mit reason-Inspektion, Hype-Token-rejection mit dem exakten Token in der Reason, Duplicate-First-Word-Aggregat-Violation, Missing-Number-Aggregat-Violation, Single-Number-Bullet reicht für pass, First-Word-Detection ignoriert trailing-Komma (`Drei,` matched `Drei`), Empty-Input → `all_rejected`+`passed=False`, Below-MIN_BULLETS aber ohne Per-Bullet-Failure trotzdem `passed=False` (template-fallback-Signal), `BulletQualityResult` ist frozen (kein nachträgliches Setzen von `passed`), Non-String-Inputs werden zu `non_string`-Rejections, Integration-Tests: Hype-Tokens lecken nicht ins HTML (`build_amazon_description_html` mit Hype-Bullets prüft dass `ultimativ`/`garantier`/`perfekt`/`geheim`/`bestseller` nicht im Output stehen), Missing-Number-Bullets erreichen nicht das HTML (Template-Pfad rennt), Duplicate-Start-Bullets erreichen nicht das HTML (Template-Pfad rennt), Hype-Token-Konstanten-Drift-Schutz (alle lowercase + non-empty + non-padded), `LLM_BULLETS_MIN_NUMBER_HITS>=1`. 828 Tests grün, kein bestehender Test gebrochen.)*

## Neu entdeckt — Anfaenger-Onboarding

- [x] **Smart-Launcher fuer BookPublisher starten.bat:** Vorher ruft die .bat blind `python gui.py` auf — Anfaenger ohne Python-Installation sieht `python is not recognized` und bricht ab. Vorher landet die EXE-Customer-Variante (`BookPublisher.exe`) im selben Ordner aber die .bat ignoriert sie komplett. Jetzt: priorisierter Launcher mit 3 Pfaden (EXE wenn vorhanden, sonst `python gui.py` wenn Python da, sonst freundliche deutsche Erklaerung mit den zwei moeglichen Loesungen — EXE-Download oder Python-Installation). Kein Python-Traceback mehr fuer den End-User. Distinkte Exit-Codes (0/1/2/3) pro Failure-Mode, `pause` an jedem Fehlerpfad damit das CMD-Fenster nicht weghuepft, `cd /d "%~dp0"` + absoluter EXE-Pfad `"%~dp0BookPublisher.exe"` damit der Launcher auch via Desktop-Verknuepfung mit fremdem "Start in"-Wert sauber laeuft. 9 Smoke-Tests in tests/test_starter_bat.py pinnen den Vertrag (EXE-vor-Python-Reihenfolge, deutsche Texte fuer beide Failure-Modes, beide Recovery-Optionen genannt, `cd /d` praesent, absoluter EXE-Pfad, keine gefaehrlichen Flags wie `rd /s /q`, `pause` vorhanden, ≥2 distinkte Error-Codes).*
- [x] **Auto-Discovery des beispielbuch-Ordners im GUI-Start:** Vorher zeigt die GUI den `default_input_path` aus `config.yaml` (Entwicklerpfad, existiert beim Kunden nicht). Der Anfaenger muss erst `Ordner waehlen` druecken und manuell zu `beispielbuch` navigieren — fuer einen ZIP-Frischkunden eine harte Huerde. Jetzt: neuer modules/launcher_paths.py mit reiner Funktion `resolve_default_input_path(config_default, *, app_dir, cwd)` die zuerst `<app_dir>/beispielbuch` probiert (Launcher-Schwester-Verzeichnis im entpackten ZIP), dann `<cwd>/beispielbuch` (Source-Repo-Fall), dann den Config-Default. gui.py nutzt jetzt `_launcher_app_dir()` (PyInstaller-frozen-aware via `sys.executable`, sonst `sys.argv[0]`) plus `Path.cwd()` als Roots — das Fenster oeffnet im Kunden-ZIP also mit `beispielbuch` schon vorausgefuellt, ein Klick auf `Pruefrunde starten` reicht. 8 Unit-Tests in tests/test_launcher_paths.py decken App-Dir-Prio, CWD-Fallback, Config-Default-Fallback, App-Dir-gewinnt-gegen-CWD, Datei-statt-Ordner-ignoriert, keine Filesystem-Seiteneffekte, app_dir==cwd-Dedup, Pathlib-Identity bei Config-Default ab.*
- [x] **Customer-LIES_MICH aktualisiert auf "ein Doppelklick, schon offen":** Der LIES_MICH.txt-Text in BookPublisher.zip beschreibt den neuen Workflow: Doppelklick startet den Launcher, der waehlt EXE oder Python automatisch; das GUI-Fenster oeffnet sich mit `beispielbuch` schon ausgewaehlt; nur `Pruefrunde starten` druecken. Zusaetzlicher Hinweis fuer den "Python fehlt"-Pfad mit Verweis auf die EXE-Version.*

## Neu entdeckt — Sample-Scan-LLM-Pass-Folgearbeit

- [x] **Opening-Rewrite-Highlight in beginner_summary:** Wenn ein LLM-Rewrite vorliegt, ihn direkt im Schwächster-Sample-Block von beginner_summary.md zeigen (`Vorschlag Eroeffnungssatz: _..._`). Aktuell landet er nur in sample_scan.md — der Autor sieht den Vorteil erst beim Klick auf das Detail-Artefakt. Sektion soll weggelassen werden wenn kein Rewrite vorhanden ist. *(modules/pipeline.py `_sample_section_dict` traegt `opening_rewrite` von sample_scan.json in den weakest-samples-Payload weiter; modules/industrial.py `_render_sample_section_line` rendert die Zeile `Vorschlag Eroeffnungssatz: _..._` direkt unter dem Fix, leerer/whitespace-only Rewrite und nicht-string-Werte werden defensiv geschluckt — kein Stub-Italics in beginner_summary; 6 neue Tests in tests/test_pipeline_helpers.py + tests/test_industrial.py decken carry-through, blank, multi-section-Renderer und non-string-Drop ab.)*
- [x] **Rewrite-Quelle in sample_scan.json mitschreiben:** Pro Section ein optionales `rewrite_source: "llm"` Feld, damit downstream Tools (Release-Packager, CI, beginner_summary-Highlight) den LLM-Pfad ohne Inferenz erkennen — analog zu `bullets_source` in amazon_description.json. *(modules/sample_scan.py: neue Konstanten `REWRITE_SOURCE_LLM` + `REWRITE_SOURCES`, `SampleSectionScore.rewrite_source: str = ""`, `to_json` emittiert das Feld nur wenn ein non-empty `opening_rewrite` UND ein non-empty `rewrite_source` vorliegen — kein Solo-Leak. `apply_sample_rewrites(..., source=REWRITE_SOURCE_LLM)` stempelt die Provenance, unknown sources fallen defensiv auf "llm" zurueck (Whitelist-Schutz). modules/pipeline.py `_sample_section_dict` traegt `rewrite_source` analog zu `opening_rewrite` weiter — nur wenn beide non-empty sind und der Wert ein non-blank string ist; non-string/whitespace-only/Source-ohne-Rewrite werden defensiv gedropped. 11 neue Tests in tests/test_sample_scan_llm.py (default empty, json omit/emit, apply-stamp, unknown-source-fallback, sources-konstante) + 4 neue Tests in tests/test_pipeline_helpers.py (carry-through, omit-when-no-rewrite, blank, non-string).)*
- [ ] **Smoke-Test für Pipeline-End-to-End mit LLM-Rewrites:** Aktuelle Tests stubben den LLMClient, aber kein End-to-End-Test fährt `run_qa` mit `sample_scan_llm_rewrites_enabled=true` + Stub-LLM gegen ein DOCX und prüft, dass `sample_scan.md` die Rewrite-Zeile enthält. Würde sicherstellen, dass die Paragraph-Stream-Re-Reading-Logik auch unter realen IO-Konditionen funktioniert.

## Neu entdeckt — Rewrite-LLM-Pass-Folgearbeit

- [ ] **Rewrite-LLM-Variante-Marker in beginner_summary:** `_top_rewrite_payload` kann jetzt eine LLM-Variante als Top-Pick wählen, aber `_render_top_rewrite` zeigt nicht, ob der Vorschlag aus dem LLM-Pass oder dem Template stammt. Eine Marker-Zeile (`Quelle: LLM-Pass`) analog zum `bullets_source`-Marker der Amazon-Vorschau würde dem Autor sagen, ob er einen manuskript-nahen Rewrite oder ein generisches Template vor sich hat. `source`-Feld liegt bereits in `rewrite_suggestions.json` pro Option — nur Payload-Carry-Through + Render-Zeile fehlen.
- [ ] **Rewrite-LLM-Pass mit Manuskript-Kontext:** Aktuell schreibt der LLM-Pass nur aus Titel/Untertitel/Beschreibung um. Analog zum Amazon-Bullet-Pass (`extract_chapter_intros`) könnten die ersten Absätze der stärksten Kapitel als zusätzlicher Kontext in den Prompt fließen, damit die Rewrites noch enger am echten Buchinhalt liegen — pure-Python-Extraktor aus chapters.py bereits vorhanden, nur Wiring im Prompt fehlt.
- [ ] **Rewrite-Variante-Qualitäts-Check:** Analog zum `validate_llm_bullets`-Gate in amazon_html.py einen Sanity-Check der LLM-Rewrites (kein doppelter Anfangssatz mit dem Original, mindestens ein Anker-Keyword behalten, keine Hype-Tokens aus `LLM_BULLETS_HYPE_TOKENS`). Bei Verstoß fällt die Variante weg statt in den Report zu rutschen. Aktuell filtert `_parse_rewrite_variants_payload` nur Ausrufezeichen + Länge.

## Produktionsreife — Robustheit (dynamischer Test-Loop)

- [x] **`complete_json` darf nie einen rohen Traceback werfen:** Der Regex-Fallback `json.loads(match.group(0))` konnte selbst eine `json.JSONDecodeError` werfen (Modell liefert geschweifte Klammern, aber kein valides JSON) → Python-Traceback statt freundlicher Meldung für den Endnutzer. Zusätzlich konnte `json.loads` ein Nicht-Dict (Liste/Skalar/`null`) zurückgeben, das als `dict[str, Any]` getypt durchrutschte und downstream bei `.get(...)` einen verzögerten `AttributeError` auslöste. *(modules/llm.py: neuer statischer Helper `_loads_json_object()` parst tolerant zu `dict | None` — fängt `JSONDecodeError`/`TypeError`/`ValueError` UND erzwingt die Dict-Form (Top-Level-Array wird nicht mehr als Dict ausgegeben). `complete_json` versucht zuerst den Volltext, dann den Regex-Treffer, und wirft bei Totalausfall einen `ConfigError` mit neuer deutscher Konstante `INVALID_JSON_MESSAGE` (Hinweis: Pruefrunde erneut starten). Greedy-`{.*}` extrahiert jetzt das innere Objekt aus `[{...}]`. 7 neue Tests in tests/test_llm.py decken sauberes Objekt, Objekt-aus-Prosa, Array→Inner-Objekt, malformed-Klammer→ConfigError, kein-JSON→ConfigError, Skalar→ConfigError und den `_loads_json_object`-Helper ab. Erste Tests für `complete_json` überhaupt — die Methode hatte zuvor null Coverage.)*
- [x] **`read_pdf_text` Robustheit angleichen an `read_docx_text`:** Drei Lücken in modules/readers.py geschlossen: (1) neuer `path_obj.is_dir()`-Zweig analog zu DOCX — zeigt der Pfad auf einen Ordner, kommt jetzt eine freundliche `ManuscriptReadError` ("Der Pfad zeigt auf einen Ordner statt auf eine PDF-Datei.") statt einer unklaren `fitz`-Exception; (2) das geöffnete `fitz`-Dokument wird jetzt in einem `finally`-Block via `doc.close()` geschlossen — kein Resource-Leak / File-Lock auf Windows mehr, auch wenn das Seiten-Lesen mittendrin scheitert; (3) der Open-Pfad UND der neue Seiten-Lese-Pfad fangen jeweils `except ManuscriptReadError: raise` VOR dem breiten `except Exception` ab — eine bereits freundliche Fehlermeldung (z.B. aus einem inneren Reader) wird nicht mehr fälschlich als "beschädigte PDF" re-wrapped. Die Seiten-Iteration war zuvor komplett ungeschützt (lief nach dem try/except) und ist jetzt in einen eigenen try/except/finally gehüllt mit der Meldung "Die Seiten der PDF-Datei konnten nicht gelesen werden: …". 8 neue Unit-Tests in tests/test_readers.py mit einem injizierten Mock-`fitz`-Modul (`_install_fake_fitz` via `monkeypatch.setitem(sys.modules, ...)`, `_FakeDocument`/`_FakePage`) — kein echter PDF-Binär- oder Netzwerk-Zugriff: Ordner-Input → freundlicher Fehler, Happy-Path joined Seiten + schließt Doc, `max_pages` respektiert + schließt Doc, Open-Failure wird gewrapped, Seiten-Lese-Failure wird gewrapped + Doc geschlossen, mittendrin geworfene `ManuscriptReadError` propagiert unverändert (Sentinel-Hint-Check) + Doc geschlossen, missing-file-Check feuert VOR `fitz.open` (Doc nie geöffnet). 922 Tests grün, 3 skips, kein bestehender Test gebrochen.)
